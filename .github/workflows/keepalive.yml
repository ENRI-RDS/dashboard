name: Keep Render Alive
on:
  schedule:
    - cron: '*/13 7-18 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -sf https://enri-dashboard-api.onrender.com/api/health || echo "ping failed"
