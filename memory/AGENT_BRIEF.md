> Documento di onboarding per agenti AI / sviluppatori. Spiega architettura, flussi, file e convenzioni della dashboard ENRI **senza dover leggere tutto il codice**.

---

## 1. Cos'è questo progetto

Dashboard di project management per **ENRI** — un progetto infrastrutturale di posa fibra/cavidotti (12 lotti, 7 cluster, province BG/MI/MB/PV/CR). Mostra l'avanzamento di:
- **Fase 1 – Progettazione**: iter autorizzativo (richieste, scadenze, pratiche per cluster/lotto/ente).
- **Fase 2 – Avanzamento Lavori (Scavi)**: stato cantieri, % completamento, metri scavati.
- **Sopralluoghi, Mappa georeferenziata, Milestone, AI Alerts**.
- **Area Impresa**: portale per le imprese appaltatrici (vista pratiche dei propri lotti + workflow di approvazione admin).

È una **dashboard solo-frontend statica** (HTML + JS vanilla, niente React/Vue) servita da **GitHub Pages**, con un **backend FastAPI** (Render) che permette di aggiornare i dataset senza re-pushare su GitHub e che — dopo ogni approvazione — committa anche la versione aggiornata direttamente nel repo.

---

## 2. Architettura a colpo d'occhio

```
┌──────────────────────────────┐    ┌────────────────────────────┐    ┌──────────────────────┐
│  GitHub Pages                │    │  Render (FastAPI)          │    │  MongoDB Atlas       │
│  enri-rds.github.io/dashboard│───▶│  enri-dashboard-api.       │───▶│  DB: enri_dashboard  │
│  HTML + JS vanilla           │    │  onrender.com              │    │   - uploads (GridFS) │
│  Pagine: hub, index, mappa,… │    │  /api/* endpoints          │    │   - assignments      │
└──────────────────────────────┘    └────────────────────────────┘    │   - pending_updates  │
        │                                       ▲                      └──────────────────────┘
        │ js/api-config.js                      │ multipart upload + workflow imprese
        │ intercetta fetch('Master.csv') e la   │
        │ riscrive in fetch(API/api/data/…)     │
        └───────────────────────────────────────┘
                                                │
                                                ▼ commit automatico (GitHub API, fine-grained PAT)
                                       Master.csv / QGIS.geojson / Riepilogo_progettazione.csv

Auth login: hub.html → POST /api/auth/login → backend chiama Google Apps Script server-to-server →
            restituisce token HMAC firmato (nome|ruolo|exp) salvato in localStorage._enri_token.
            Ogni chiamata /api/imprese/* o /api/auth/* richiede l'header `x-session-token`.
```

**Tre modi di girare:**
1. **Production**: GitHub Pages + Render + Atlas.
2. **Preview**: static HTML servito da `frontend/server.js` su :3000, FastAPI su :8001, MongoDB locale.
3. **Solo statico**: GitHub Pages senza backend (le pagine fanno `fetch('Master.csv')` direttamente sui file committati nel repo — sempre presenti perché il backend ri-pusha dopo ogni approvazione).

---

## 3. Struttura del repository

```
/app/
├── hub.html                    # PORTALE — entrypoint con login (via /api/auth/login) + griglia di card
├── index.html                  # FASE 1 — Progettazione (KPI, GANTT, tabella pratiche) — SWR su Master.csv
├── scavi.html                  # FASE 2 — Avanzamento scavi (lotti, cluster, donut)
├── mappa.html                  # Mappa Leaflet con SWR su QGIS / QTS / SED / Master
├── ~~mappa_impresa.html~~         # RIMOSSA (rev.126) — sola-vista, superset da mappa_impresa_caricamento.html. File non più nel repo.
├── ~~executive_summary.html~~     # RIMOSSA — non più nel progetto. Il pulsante "Executive" è stato rimosso dalla topbar di index.html. Nessun CSS residuo né link attivi.
├── sopralluoghi.html           # Redazione verbali di sopralluogo cantiere
├── milestone.html              # Milestone di progetto
├── ai_alerts.html              # Beta — alert predittivi
├── admin.html                  # PANNELLO ADMIN — upload + coda imprese + assegnazioni + storico versioni
├── imprese.html                # PORTALE IMPRESE — aggiorna pratiche / nuova tratta / mie submission
├── imprese_scavi.html          # Area Impresa: avanzamento scavi giornaliero (stato cantiere, metri, log)
├── mappa_impresa_caricamento.html  # Area Impresa: mappa Leaflet filtrata sui lotti assegnati, con possibilità di aggiornare/inserire pratiche direttamente dalla tratta selezionata
├── polizze_convenzioni.html    # NEW — pratiche con CONVENZIONE/POLIZZA richiesta: filtro lotto/impresa/stato + KPI aggregati
│
├── js/
│   └── api-config.js           # **CRUCIALE** — intercetta fetch() e li reindirizza al backend
│
├── Master.csv                  # Dataset principale pratiche — separatore auto-rilevato (TAB o ;)
├── Riepilogo_progettazione.csv # Riepilogo cluster — RIGENERATO automaticamente da Master.csv
├── dati.csv                    # Dati ausiliari (in disuso — index.html calcola la stessa aggregazione lato client)
├── QGIS.geojson                # Tracciato lotti — RIGENERATO automaticamente (properties di stato) da Master.csv
├── QTS.geojson                 # Tracciato secondario
├── SED_classificato.geojson    # Attraversamenti (Stradali / Ferroviari / Idrici)
│
├── backend/
│   ├── server.py               # FastAPI app v2.0.0 — vedi §5
│   ├── requirements.txt        # include httpx (chiamate GitHub API + Apps Script)
│   ├── .env                    # MONGO_URL, DB_NAME, UPLOAD_TOKEN, SESSION_SECRET, APPS_SCRIPT_*, GITHUB_*
│   └── .env.example
│
├── frontend/                   # Server statico per preview Emergent (NON in produzione)
│   ├── server.js
│   └── package.json
│
├── DEPLOY.md                   # Guida deploy Render + Atlas + GitHub Pages
├── Procfile                    # `web: uvicorn server:app --host 0.0.0.0 --port $PORT`
└── memory/
    ├── PRD.md                  # Stato avanzamento del progetto
    ├── test_credentials.md     # Credenziali backend per preview
    └── AGENT_BRIEF.md          # ← QUESTO FILE
```

---

## 4. Pagine — cosa fa ognuna

| Pagina | Ruolo necessario | Cosa mostra / fa |
|---|---|---|
| **hub.html** | nessuno (login obbligatorio) | Login server-side (POST `/api/auth/login`) + griglia card. Mostra card "Area Impresa" solo se backend conferma assegnazione. Anti-FOUC: card nascoste finché non torna il ruolo. Cold-start Render: timeout 10s e pannello "Riprova" |
| **index.html** | `admin`/`admin2`/`user` (tutti tranne `impresa`) | KPI, GANTT, tabella filtrabile, modal pratiche, grafici Chart.js. **SWR**: legge GitHub statico + Render in parallelo, ridisegna live se il backend ha dati più freschi. XLSX caricato lazy al primo "Esporta Excel" |
| **scavi.html** | `admin`/`admin2`/`user` (tutti tranne `impresa`) | KPI scavi (non avviati/in corso/sospeso/completati) con metri+% sul totale (rev.8), barre per lotto e per cluster a segmenti multi-stato reali (rev.8, non più blob binario), donut chart, modal lotto/cluster, tabella "Tutti i Cantieri" con `codice_cantiere`/`impresa` (rev.10: rimossa colonna `tecnica_scavo`, resta solo nella card modal). ⚠️ Ruolo cambiato: in precedenza era ristretto a `admin`/`admin2`, ora `SCAVI_ALLOWED_ROLES` include anche `user`. ✅ Topbar uniformata al brand kit Retelit (§8.11 pattern: navy, logo base64, `.topbar-back`) — sostituita la vecchia topbar chiara con `.logo`/`.btn-nav`. Rimosso il badge "Struttura reale · avanzamento di esempio". **(sessione 2026-07-03)**: exec-strip con 3 KPI grandi (avanzamento fisico/permessi/sospesi) in header; titolo/eyebrow/sottotitolo pagina rimossi su richiesta utente, `#pageSubDyn` di nuovo assente dal markup |
| **mappa.html** | `admin`/`admin2`/`user` (tutti tranne `impresa`) | Leaflet — SWR su `QGIS.geojson`, `QTS.geojson`, `SED_classificato.geojson`, `Master.csv`; ricostruzione live dei layer se cambiano. Basemaps Google-style, ricerca, misurazione distanze, esportazione PDF |
| **mappa_impresa_caricamento.html** | impresa loggata | Mappa filtrata sui lotti assegnati (usa `/api/imprese/pratiche` e session token), con possibilità di aggiornare lo stato di una pratica o inserirne una nuova direttamente cliccando sulla tratta (`/api/imprese/submit`, `/api/imprese/my-submissions`, `/api/imprese/cantieri*`). Include il blocco "tracking accessi" (vedi §5.9). Unica pagina mappa lato impresa (sostituisce l'ex `mappa_impresa.html`, rimossa rev.126) |
| **milestone.html** | `admin`/`admin2`/`user` (tutti tranne `impresa`) | Milestone contrattuali e di impresa |
| **sopralluoghi.html** | `admin`/`admin2`/`user` (tutti tranne `impresa`) | Form di redazione verbale. ⚠️ Non è più solo client-side: i verbali sono ora persistiti su MongoDB (`POST/GET/DELETE /api/sopralluoghi`) con codice progressivo `VBS-AAAA-NNNN` e foto caricate su GitHub (`sopralluoghi/foto/{codice}/`) invece che generare solo un PDF locale |
| **ai_alerts.html** | tutti (Beta) | Pagina placeholder per future analisi AI |
| **polizze_convenzioni.html** | `admin`/`admin2`/`user` (tutti tranne `impresa`) | Pratiche con CONVENZIONE e/o POLIZZA richiesta: filtro per lotto/impresa/stato, KPI aggregati. Legge `/api/admin/polizze-convenzioni/data-richiesta`, con fallback diretto a Master.csv su GitHub Pages se l'API non risponde. ✅ **RISOLTO**: guardia di login (`_enri_user`) aggiunta, stesso pattern overlay usato in scavi.html/sopralluoghi.html. La sola scrittura (`POST /api/admin/polizze-convenzioni/update`) richiede un `UPLOAD_TOKEN` chiesto al volo via modale, salvato in `localStorage['enri_upload_token']` — stessa chiave di admin.html (§8.15) |
| **admin.html** | richiede `UPLOAD_TOKEN` | 4 tabs (File correnti, Storico versioni, **Coda imprese**, **Assegnazioni imprese**). Badge live conteggio pending, modali HTML custom (no `alert/confirm/prompt` nativi), date in formato `gg/mm/aa, hh:mm`, righe coda colorate per tipo. Upload: select `target` a scelta fissa (no testo libero), JS blocca l'invio se il nome del file selezionato dal disco non combacia (case-insensitive) col `target` scelto — messaggio "Nome file non corrispondente" (riga ~842) |
| **imprese.html** | impresa assegnata + session token | 3 tabs (Aggiorna pratiche / Nuova pratica / Le mie submission). Campo **Ente** = select dinamica via `/api/enti` + opzione "Altro"; **Pratica** in nuova tratta = solo numero progressivo editabile (prefisso AUT/NO + suffisso lotto calcolati). Selezionando una tratta, se altre tratte condividono ENTE+TIPO+PRATICA il sistema propone di aggiornarle tutte insieme |
| **imprese_scavi.html** | impresa assegnata + session token | "Avanzamento Scavi" lato impresa: aggiornamento giornaliero per pratica/cantiere (stato cantiere, tecnica di scavo, date inizio/fine, metri realizzati **oggi** — accumulati su `metri_scavati`), storico log consultabile. Scrittura diretta, **senza** workflow di approvazione admin (a differenza di `imprese.html`) |

Tutte le pagine fanno guardia: `if (!localStorage.getItem('_enri_user')) → mostra overlay/redirect a hub.html`. Le pagine impresa controllano anche `localStorage._enri_session`.

⚠️ **Rinominata la chiave di sessione**: il token firmato in localStorage non si chiama più `_enri_token` ma **`_enri_session`** (verificato in hub.html, mappa_impresa_caricamento.html). Se trovi ancora `_enri_token` in qualche file non aggiornato, è codice vecchio.

---

## 5. Backend FastAPI (`backend/server.py` v2.0.0)

**Modello di storage**: MongoDB GridFS è AUTORITATIVO per i file caricati. I CSV/GeoJSON
nel repo (`/app/Master.csv`, ecc.) sono usati come SEED FALLBACK quando non esiste
ancora un upload per quel nome. Dopo ogni approvazione/eliminazione/ripristino il
backend pusha anche su GitHub via API (token fine-grained, scope `Contents: Read and write`)
così la copia statica del repo non va più in deriva da quella servita live.

**Tutte le rotte iniziano con `/api/`**.

### 5.1 File / dataset

| Metodo | Endpoint | Auth | Descrizione |
|---|---|---|---|
| GET | `/api/` | — | Health JSON di servizio |
| GET | `/api/health` | — | Ping a MongoDB |
| GET | `/api/files` | session token, **solo staff** | Lista unificata: Mongo + seed disco. Ogni file: `source: 'mongo'\|'disk'`, `versions`, `size`, `modified` |
| GET | `/api/data/{path}` | session token | Scarica file (Mongo se presente, altrimenti disco). Usato da `js/api-config.js` |
| GET | `/api/data-text/{path}` | session token | Idem in `text/plain` |
| GET | `/api/preview/{path}?max_bytes=N` | session token, **solo staff** | Anteprima primi N byte (256–65536, default 8192) |
| GET | `/api/uploads?limit=&project=&filename=&include_deleted=` | session token, **solo staff** | Storico upload (filtri + audit cancellati) |
| POST | `/api/upload` | `UPLOAD_TOKEN` (form `token` o header `x-upload-token`) | Multipart: `file`, `target` opz., `project`, `convert_to_csv`. Salva in GridFS + record `uploads`. Excel → CSV auto. **Se il target è `Master.csv` pusha su GitHub e rigenera QGIS/Riepilogo**. ⚠️ Se `target` è vuoto, `out_name` ricade sul filename originale del file caricato (`server.py` riga ~359) — da `admin.html` non capita mai (guardia lato client, v. riga 110), ma qualunque altro chiamante di questo endpoint senza passare `target` esplicito rischia di salvare sotto nome sbagliato senza errore. `GET /api/data/{filename}` e `GET /api/files` selezionano sempre l'upload con `uploaded_at` più recente per quel filename (nessun lock/versioning ottimistico tra scritture concorrenti, v. §8.41) |
| DELETE | `/api/uploads/{id}` | `UPLOAD_TOKEN` | Soft-delete singola versione + purge GridFS. Se era Master.csv ri-pusha lo stato corrente |
| DELETE | `/api/files/{path}` | `UPLOAD_TOKEN` | Soft-delete di TUTTE le versioni — se esiste un seed disco, torna a essere servito |
| PATCH | `/api/files/{old}` | `UPLOAD_TOKEN` | Body JSON `{"new_name": "..."}` (409 se collide) |
| POST | `/api/uploads/{id}/restore` | `UPLOAD_TOKEN` | Ripristina versione cancellata (410 se i byte GridFS sono già stati purgati) |
| GET | `/api/enti` | session token | NEW — elenco enti unici presenti in Master.csv, ordinati alfabeticamente (popola la select "Ente" in imprese.html) |

### 5.2 Auth firmata (HMAC)

Risolve la falla per cui chiunque poteva fare `localStorage.setItem('_enri_user', 'Nome Impresa')`
e impersonare quel nome senza conoscere il codice. Ora il codice è verificato dal backend e ogni
chiamata successiva richiede un token firmato non falsificabile.

| Metodo | Endpoint | Auth | Descrizione |
|---|---|---|---|
| POST | `/api/auth/login` | — | Body `{nome, codice}` → backend chiama Apps Script server-to-server (segreto MAI esposto al browser) → restituisce `{ok, token, nome, ruolo}` |
| GET | `/api/auth/verify` | session token | Rivalidazione silenziosa (usata da index.html) |
| POST | `/api/logs/get` / `/api/logs/put` | session token | Log accessi — MongoDB (`access_logs`), non più JSONBin/Apps Script (vedi §5.12) |

**Schema token** (base64 urlsafe): `nome | ruolo | exp_unix | HMAC-SHA256(payload, SESSION_SECRET)`
con TTL configurabile via `SESSION_TTL_SECONDS` (default 12h).

### 5.3 Imprese (richiedono `x-session-token` — il `nome` è SEMPRE preso dal token, MAI da un parametro client)

> Nota: l'header resta `x-session-token`; è solo la chiave **localStorage** lato client che è stata rinominata da `_enri_token` a `_enri_session` (vedi §4 e §7).

| Metodo | Endpoint | Descrizione |
|---|---|---|
| GET | `/api/imprese/me` | Profilo + lotti assegnati (404 se non assegnata) |
| GET | `/api/imprese/pratiche` | Master.csv filtrato per i lotti dell'impresa. Confronto per **codice lotto esatto** (non substring — "Lotto 2" non aggancia "Lotto 2A") |
| POST | `/api/imprese/submit` | Body `{type:'update'\|'new', changes:[...]}` → record in `pending_updates`, status `pending` |
| GET | `/api/imprese/my-submissions` | Storico delle proprie submission |
| DELETE | `/api/imprese/submissions/{id}` | L'impresa cancella SOLO le proprie submission ancora `pending` |
| GET | `/api/lotti-cantieri` | NEW — lotti distinti (da Master.csv) + cantieri associati (da MongoDB) + mappa lotto→impresa assegnata. Usato per popolare select a cascata |
| GET | `/api/imprese/cantieri` | NEW — cantieri (uno per pratica di autorizzazione) nei lotti dell'impresa autenticata |
| POST | `/api/imprese/cantieri/{cantiere_key}` | NEW — aggiorna stato cantiere, tecnica scavo, date, **metri_realizzati_oggi** (si accumula su `metri_scavati`, non sovrascrive), note. Push automatico su GitHub. **Scrittura diretta, nessuna approvazione admin** (a differenza di `/api/imprese/submit`) |
| GET | `/api/imprese/cantieri/{cantiere_key}/log` | NEW — storico aggiornamenti giornalieri di un cantiere |
| GET | `/api/imprese/solleciti` | NEW — solleciti dell'impresa, filtrati per le tratte dei suoi lotti |
| POST | `/api/imprese/solleciti` | NEW — inserisce un sollecito. Scrittura diretta, nessuna approvazione |
| POST | `/api/imprese/solleciti/bulk-insert` / `bulk-delete` | NEW — inserimento/cancellazione massiva |
| DELETE | `/api/imprese/solleciti/{id}` | NEW — elimina un sollecito |

### 5.4 Admin (richiedono `UPLOAD_TOKEN`)

| Metodo | Endpoint | Descrizione |
|---|---|---|
| GET / PUT / DELETE | `/api/admin/assignments[/{nome}]` | CRUD nome impresa ↔ lotti (lookup case-insensitive) |
| GET | `/api/admin/pending-updates?status=pending\|approved\|rejected\|all` | Lista coda |
| POST | `/api/admin/pending-updates/{id}/approve` | Applica le modifiche a Master.csv (nuova versione GridFS), **rigenera QGIS.geojson + Riepilogo_progettazione.csv** e pusha tutti e tre su GitHub. Da qui parte anche `_sync_cantieri()` (vedi §5.12) |
| POST | `/api/admin/pending-updates/{id}/reject` | Body `{note}` |
| GET | `/api/admin/polizze-convenzioni/data-richiesta` | NEW — per ogni pratica con CONVENZIONE/POLIZZA valorizzata, fissa (una sola volta, `$setOnInsert`) la data di prima richiesta nella collection `pol_conv_dates` |
| POST | `/api/admin/polizze-convenzioni/update` | NEW — Body `{lotto, pratica, fields:{CONVENZIONE?, POLIZZA?}}`. Valori ammessi: `NECESSARIA\|RICHIESTA RDS\|INVIATA\|OTTENUTA\|""`. Scrive su Master.csv (tutte le righe lotto+pratica) e pusha su GitHub |
| GET | `/api/admin/sync-cantieri` | NEW — forza la sincronizzazione cantieri↔Master.csv (crea cantieri mancanti, uno per pratica AUTORIZZAZIONE) |
| GET | `/api/admin/solleciti` | NEW — vista admin di tutti i solleciti |

### 5.5 Auto-push GitHub + rigenerazione derivati

`_push_to_github(file_bytes, path, label)` — commit via API GitHub (GET sha → PUT contents), fire-and-forget, retry su conflitto sha (409) fino a 3 volte. Skip silenzioso se `GITHUB_TOKEN` mancante: MongoDB resta comunque autoritativo.

`_regenerate_derived_files(master_df, note)` — Scoperta chiave: `Riepilogo_progettazione.csv` è esattamente la tabella attributi di `QGIS.geojson` esportata in CSV (stesse 21 colonne, stesso ordine, stessi valori, verificato riga per riga). Per questo:

```
Master.csv (dinamico)
    │
    ▼
_compute_tratta_summary() — per ogni TRATTA_ID:
   • STATO_AUTORIZZAZIONE ← ultima riga AUTORIZZAZIONE attiva (le pratiche
     con STATO_PERMESSO=NO COMPETENZA vengono saltate se esistono alternative)
   • STATO_NULLAOSTA / STATO_ORDINANZA ← peggiore tra l'ultimo stato di ciascun ente
   • LAVORABILE ← SI solo se AUTORIZZAZIONE=OTTENUTO e, se richiesti,
     anche NULLA OSTA/ORDINANZA=OTTENUTO
   • PRATICA ← "AUT/24/1A | NO/22/1A | …" (AUT, poi NO, poi ORD)
    │
    ▼
PATCH UNICA sulle properties di QGIS.geojson (geometria invariata)
    │
    ▼
Riepilogo_progettazione.csv = derivato direttamente da quelle properties
```

Campi **mai derivati automaticamente** (provenienti da sistemi esterni o senza regola certa): `CAMPO AWS`, `PROTOCOLLO_AUT`, `ENTE 2`, `fid`, `TIPOLOGIA`, `PROVINCIA`, `ROUTE`, `SPAN`. Restano quelli già presenti in QGIS.geojson.

### 5.6 Approvazione: come viene applicata una `update`

`_apply_changes_to_df`:
1. Match per `TRATTA_ID + ENTE + TIPO_PERMESSO` (+ `PRATICA` se fornita come discriminante: utile quando sulla stessa tratta+ente+tipo esistono pratiche diverse).
2. **Copia l'ultima riga esistente** e la inserisce **subito DOPO** la stessa (non in fondo al file) — così le righe dello stesso iter restano vicine.
3. Auto-set `DATA_ULTIMA_MODIFICA = oggi` se non fornito e c'è un cambio di `STATO_PERMESSO`.
4. Per le submission `type:'new'` aggiunge una riga vuota popolata con i campi inviati.

### 5.7 Variabili d'ambiente (`backend/.env`)

| Key | Significato |
|---|---|
| `MONGO_URL` | URI Mongo (locale o Atlas) |
| `DB_NAME` | `enri_dashboard` |
| `DATA_DIR` | Cartella seed (default: parent di `/backend`, in preview `/app`) |
| `ALLOWED_ORIGINS` | CSV di origini CORS |
| `UPLOAD_TOKEN` | Token admin (upload/delete/assignments/approve). Generare con `openssl rand -hex 32` |
| `MAX_UPLOAD_MB` | Default `25` |
| `SESSION_SECRET` | **OBBLIGATORIO** — chiave HMAC per i session token (`openssl rand -hex 32`) |
| `SESSION_TTL_SECONDS` | Default `43200` (12h) |
| `APPS_SCRIPT_URL` | URL del Google Apps Script di login |
| `APPS_SCRIPT_SECRET` | Segreto condiviso con Apps Script (MAI esposto al client) |
| `GITHUB_TOKEN` | Fine-grained PAT, repo `ENRI-RDS/dashboard`, `Contents: Read and write`. Se assente il push viene skippato |
| `GITHUB_REPO` | Default `ENRI-RDS/dashboard` |
| `GITHUB_BRANCH` | Default `main` |
| `GITHUB_CSV_PATH` / `GITHUB_QGIS_PATH` / `GITHUB_RIEPILOGO_PATH` | Override path nel repo se servono sottocartelle |

### 5.8 Vincoli di sicurezza
- `_safe_relpath()` impedisce path-traversal (`..`, `\`, caratteri fuori `[A-Za-z0-9_\-./]`).
- Estensioni accettate: `.csv`, `.xlsx`, `.xls`, `.geojson`, `.json`.
- Limite dimensione = `MAX_UPLOAD_MB`.
- HMAC compare con `hmac.compare_digest` (timing-safe).
- Le rotte impresa **ignorano qualunque parametro `nome`** passato dal client e usano sempre quello firmato nel token.

### 5.9 Sopralluoghi (collection `sopralluoghi`)

| Metodo | Endpoint | Auth | Descrizione |
|---|---|---|---|
| GET | `/api/sopralluoghi` | session token | Tutti i verbali, ordinati per `codice_verbale` decrescente |
| GET | `/api/sopralluoghi/next-codice` | session token | Prossimo codice progressivo `VBS-{anno}-{NNNN}` |
| POST | `/api/sopralluoghi` | session token | Salva verbale su MongoDB + aggiorna `sopralluoghi.csv` su GitHub. Le foto (data URL base64) vengono caricate su GitHub in `sopralluoghi/foto/{codice}/` per non saturare lo storage Mongo gratuito |
| DELETE | `/api/sopralluoghi/{id}` | session token, **solo ruolo `admin`** | Elimina un verbale |

### 5.10 Cantieri / Avanzamento Scavi impresa (collection `cantieri`)

Un cantiere = una pratica AUTORIZZAZIONE (chiave `cantiere_key`, **non** `pratica_id`, perché quest'ultimo non è garantito univoco tra enti diversi sullo stesso lotto/numero).

- **`codice_cantiere`** (`CA/{progressivo}/{lotto}`, rev.5): identificativo "ufficiale" mostrato all'impresa e in `scavi.html`, distinto da `cantiere_key` (chiave tecnica) e `pratica_id` (riferimento pratica). Assegnato **una sola volta** alla creazione in `_sync_cantieri()`, mai riassegnato; `_max_codice_per_lotto()`+`_backfill_codici_cantiere()` garantiscono continuità anche sui cantieri storici. Esposto su `/api/cantieri`, `/api/imprese/cantieri`, `cantieri.csv`.
- ✅ **RISOLTO (rev.10)**: `impresa` sui cantieri era popolato **solo** da migrazione best-effort dal vecchio schema pre-raggruppamento (`old_docs`) — per qualsiasi cantiere creato dopo, restava sempre `""`. `_sync_cantieri()` ora costruisce all'inizio una mappa `lotto_impresa` dalla collection `assignments` (stessa fonte/logica di `GET /api/lotti-cantieri`) e la usa per popolare `impresa` sia in creazione sia in update (backfill automatico sui cantieri esistenti privi del campo, senza sovrascrivere se un lotto non ha assegnazione).
- **Transizioni stato** (`STATO_TRANSITIONS`, JS, rev.5): la select stato in `mappa_impresa_caricamento.html`/`imprese_scavi.html` mostra solo stato corrente + transizioni valide (`non_avviato→allestimento→in_corso→{sospeso,completato}`, `sospeso→in_corso`). Su `allestimento`: `tecnica_scavo`/`metri_realizzati_oggi` disabilitati e non richiesti (né in UI né in payload). `data_inizio_effettiva` lockata permanentemente una volta valorizzata.
- `GET /api/cantieri?lotto=&cluster=&stato=` — lista pubblica, usata da `scavi.html` (lo storico `log` viene tolto dal listing per non appesantire la risposta).
- L'impresa aggiorna via `POST /api/imprese/cantieri/{cantiere_key}` (vedi §5.3): **scrittura diretta senza approvazione**, a differenza del flusso `imprese.html`/`pending_updates`. Ogni update accumula `metri_realizzati_oggi` su `metri_scavati` (`$inc`) e appende una riga a `log[]` (data, impresa, stato, **tecnica_scavo** [rev.6], metri, note, motivo blocco). ⚠️ Bug corretto (rev.5): il check assegnazione confrontava `lotto` non normalizzato (`"1A"` vs `"Lotto 1A"`) → 403 spurio; ora entrambi i lati passano da `_lotto_from_source()`.
- `_sync_cantieri()` crea i cantieri mancanti a partire da Master.csv; viene rilanciata sia da `/api/admin/sync-cantieri` sia automaticamente dopo ogni approvazione di `pending_updates`. **`metri_totali` = somma di TUTTE le tratte della pratica (autorizzazione ottenuta), indipendentemente da `lavorabile`** (rev.9, §8.33): quel flag per-tratta è solo indicativo per la mappa (NULLA OSTA/ORDINANZA ottenuti), non deve e non limita cosa l'impresa può rendicontare come scavato.
- Dopo ogni scrittura, push fire-and-forget su GitHub (`_push_cantieri_to_github`).

### 5.11 Solleciti (collection `solleciti`)

Sistema di "promemoria/follow-up" per pratiche in attesa, associato alle tratte dei lotti dell'impresa. Scrittura diretta (no workflow di approvazione), CRUD completo lato impresa (`/api/imprese/solleciti*`) e vista aggregata lato admin (`/api/admin/solleciti`).

### 5.12 ✅ Tracking accessi — RISOLTO + migrato da JSONBin a MongoDB

Tutte le 4 pagine con blocco `<!-- TRACKING ACCESSI -->` (`scavi.html`, `mappa.html`, `mappa_impresa_caricamento.html`, `sopralluoghi.html`) chiamano `POST /api/logs/get` e `POST /api/logs/put` (header `x-session-token`). **Il backend non chiama più Apps Script/JSONBin per questo**: legge/scrive dalla collection Mongo `access_logs` (un documento per `binId`, campi `utenti[]`/`accessi[]`). Contratto richiesta/risposta lasciato identico apposta (`{binId}` → `{record:{utenti,accessi}}`), quindi **le pagine non sono state toccate di nuovo** — solo il backend è cambiato.

`APPS_SCRIPT_URL`/`APPS_SCRIPT_SECRET` restano in uso **solo per il login** (`action: "login"` verso il Google Sheet), non più per i log.

⚠️ **Dati storici non migrati**: gli accessi già registrati sul vecchio bin JSONBin non sono stati copiati automaticamente su MongoDB (nessun accesso di rete disponibile per farlo in questa sessione). Se serve conservare lo storico, va fatto un import una tantum leggendo il bin esistente e scrivendolo in `access_logs`. Da questo deploy in poi, il log riparte vuoto su Mongo.

`index.html` conteneva anche codice/commenti morti che nominavano JSONBin per una funzione "Note per pratica" già disattivata (stub `noteLoad`/`noteSave` no-op) e per due commenti descrittivi non più accurati — ripuliti (rinominati, nessuna funzionalità toccata).

---

## 6. Come i file caricati arrivano sul frontend — `js/api-config.js`

Ogni pagina HTML include `<script src="js/api-config.js"></script>` in `<head>`. Lo script:

1. Legge `window.ENRI_API_BASE` **oppure** `localStorage.getItem('enri_api_base')`.
2. Se vuoto → non fa nulla, le pagine leggono i CSV/GeoJSON statici committati nel repo.
3. Se valorizzato → **monkey-patcha `window.fetch`**: ogni `fetch('Master.csv')` viene riscritta in `fetch(`${API_BASE}/api/data/Master.csv`)`.

**Pattern SWR (stale-while-revalidate)** in `index.html`, `mappa.html`, `mappa_impresa_caricamento.html`:
- fetch parallelo del file statico GitHub (istantaneo, sempre disponibile) E del backend Render (può essere lento al cold-start);
- la UI mostra subito il dato statico;
- se Render risponde con un contenuto diverso, **la UI si aggiorna live senza reload** (callback ridisegna tabella, ricostruisce layer Leaflet, ecc.).

**Flusso completo upload/approvazione:**
1. Impresa apre `imprese.html` (auth: token in `_enri_session`), compila modifiche, invia.
2. Backend salva in `pending_updates` (status `pending`).
3. Admin apre `admin.html` → tab "Coda imprese" (badge live) → Approva.
4. Backend: legge Master.csv corrente → applica `changes` con pandas (riga inserita dopo l'ultima della stessa pratica) → crea nuova versione GridFS → **rigenera QGIS.geojson + Riepilogo_progettazione.csv** → pusha tutti e tre su GitHub.
5. Tutte le pagine, alla prossima `fetch('Master.csv')`, ricevono la nuova versione dal backend; le SWR aggiornano la UI live senza reload.

---

## 7. Auth & ruoli

### 7.1 Login utente (hub.html → /api/auth/login)
Implementato come **proxy server-to-server** verso Google Apps Script. Il browser invia `{nome, codice}`; il backend verifica con Apps Script (segreto MAI esposto) e firma un token HMAC che viene salvato in localStorage:

```js
localStorage._enri_user    // nome canonico
localStorage._enri_role    // 'admin' | 'admin2' | 'user' | 'impresa'
localStorage._enri_session // session token firmato (HMAC, scade dopo SESSION_TTL_SECONDS) — ⚠️ rinominata da _enri_token
```

L'admin gestisce gli accessi modificando lo sheet Google collegato all'Apps Script (requisito esplicito del committente).

In `hub.html` tre liste filtrano le card:
- `SCAVI_ALLOWED_ROLES = ['admin', 'admin2', 'user']` → mostra/blocca card "Avanzamento Lavori". ⚠️ Ora include anche `user`, non più solo admin
- `ADMIN_ALLOWED_ROLES = ['admin', 'admin2']` → mostra/nasconde card "Pannello Admin".
- `IMPRESA_ROLES = ['impresa']` → attiva la modalità "Area Impresa": nasconde tutte le card normali e mostra fino a 3 card dedicate (`impresaCardsWrap`), ciascuna condizionata a `display:none` finché non si conferma l'assegnazione via `/api/imprese/me`:
  - `impresaCard` → `imprese.html` (aggiorna pratiche)
  - `impresaMapUpdCard` → `mappa_impresa_caricamento.html` (mappa con aggiornamento pratiche)
  - `impresaScaviCard` → `imprese_scavi.html` (avanzamento scavi)

Nota minor: la chiamata `fetch(apiBase + '/api/imprese/me?nome=' + ...)` in `hub.html` passa ancora un parametro `?nome=` in query string, ma il backend (`impresa_me`) lo ignora completamente e legge sempre il nome dal token firmato — il parametro è innocuo ma andrebbe rimosso dal client per coerenza con quanto dichiarato in §7.3.

### 7.2 Upload backend e azioni admin
Tutte le scritture admin (`POST /api/upload`, `DELETE /api/uploads/*`, `PUT /api/admin/assignments/*`, `POST /api/admin/pending-updates/*/approve|reject`, `POST /api/admin/polizze-convenzioni/update`, `GET /api/admin/sync-cantieri`) richiedono `UPLOAD_TOKEN`. In `admin.html` l'admin lo incolla manualmente nel form.

### 7.3 Azioni impresa
Tutti gli endpoint `/api/imprese/*` richiedono header `x-session-token`. Il `nome` viene SEMPRE letto dal token firmato, mai da un parametro `?nome=` lato server (vedi nota sopra: il client a volte lo invia ancora, ma viene ignorato).

---

## 8. Convenzioni & gotchas

1. **Separatore CSV auto-rilevato**: `Master.csv` può essere tab o `;` (Excel italiano vs export). `_detect_sep()` lo determina sulla prima riga; lettura/scrittura lo preservano. Il push su GitHub usa SEMPRE tab (formato originale del file nel repo).
2. **Encoding-robust**: Master.csv viene letto provando `utf-8`, `cp1252`, `latin-1` prima di fallback a `errors='replace'`. Le righe malformate (es. virgola non quotata in NOTE) usano `on_bad_lines='warn'` invece di fallire.
3. **CORS**: il backend espone solo le origini in `ALLOWED_ORIGINS`. Aggiungere il dominio preview/Render quando cambia.
4. **Path con sotto-cartelle**: `target=M/QGIS_3.geojson` salva in `DATA_DIR/M/QGIS_3.geojson`. La regex `_SAFE_NAME_RE` accetta `/` ma rifiuta `..`.
5. **Render free tier**: il servizio si spegne dopo 15min di inattività, prima chiamata ~30s di cold-start → frontend usa timeout 10s + pannello "Riprova". ⚠️ **`.github/workflows/keepalive.yml` abbandonato (rev.28)**: gli scheduled workflow di GitHub Actions non sono affidabili al minuto (ritardi 10-15+min documentati, causavano comunque sleep intermittenti) — sostituito con **cron-job.org** (ping `/api/health` ogni 13min, precisione al minuto, alert email su fallimento). Il file su GitHub resta solo con `workflow_dispatch` per test manuali, `schedule` rimosso. ✅ Confermato attivo in orario lavorativo (2026-07-06) — zero cold-start diurno per le imprese.
6. **AbortController → Promise.race**: l'anteprima Claude non supporta la clonazione di `AbortSignal` via `postMessage`; i timeout sui fetch usano `Promise.race` ovunque (compatibile sia in anteprima che in produzione).
7. **NO COMPETENZA**: le pratiche AUTORIZZAZIONE chiuse con STATO_PERMESSO=NO COMPETENZA sono "superate" — se esiste una pratica attiva successiva sulla stessa tratta, è quella a contare; se NO COMPETENZA è l'unica presente, la tratta è IN ATTESA della prossima pratica.
8. **Conflitti GitHub (409)**: `_push_to_github` rilegge lo sha aggiornato e riprova fino a 3 volte se qualcuno scrive sullo stesso file in parallelo.
9. **Lookup impresa case-insensitive**: `_find_assignment` cerca con regex `^nome$` case-insensitive — `sertori`, `Sertori`, `SERTORI` trovano tutti lo stesso record.
10. **`dati.csv` è in disuso**: `index.html` calcola la stessa aggregazione LOTTO×STATO×Metri/Percentuale lato client da `Riepilogo_progettazione.csv` (variabili `aggMap`/`totMap` in `loadData()`).
11. **Rebranding "Retelit" — COMPLETATO su TUTTE le pagine (rev.3)**: topbar navy (`--retelit-blue`, 58px, padding 28px) con logo Retelit embeddato come base64 bianco (50px) su tutte le pagine: `hub`, `index`, `mappa`, `sopralluoghi`, `polizze_convenzioni`, `milestone`. Bottone Hub unificato come `.topbar-back` (bordo `--retelit-sky`, hover trasparente). `<title>` aggiornati a `Retelit — …` su tutte le pagine. Tutti i token brand CSS (`--retelit-blue-50`, `--retelit-ice`, `--retelit-sky`, `--accent2`, `--border2`, `--muted2`, `--text2`, `--radius-*`, `--shadow-*`, `--dur-*`, `--ease-*`) definiti nel `:root` di ogni file. Emoji vietate dal brand kit rimosse da topbar, placeholder, export buttons e JS (restano solo simboli funzionali in pannelli tecnici Leaflet).
12. **Token rinominato**: `_enri_token` → `_enri_session` in localStorage (vedi §7.1). Se incontri ancora `_enri_token` in una pagina, è codice non aggiornato.
13. **Scrittura diretta vs workflow di approvazione**: occhio a non confondere i due modelli — `imprese.html`/`mappa_impresa_caricamento.html` (pratiche) passano sempre da `pending_updates` + approvazione admin; `imprese_scavi.html` (cantieri) e i solleciti scrivono **direttamente** su MongoDB/GitHub senza step di revisione.
14. ✅ **RISOLTO** — Tracking accessi con segreto esposto: vedi §5.12. Tutte le 5 pagine ora usano il proxy `/api/logs/*`, nessun secret in chiaro nel client.
15. ✅ **RISOLTO** — `polizze_convenzioni.html` ora legge/scrive il token nella stessa chiave `localStorage['enri_upload_token']` usata da `admin.html` (prima usava `sessionStorage['_enri_upload_token']`). Se il token è già stato inserito una volta in una delle due pagine, l'altra non lo richiede più. Scelta deliberata: si è mantenuto il modello a doppio secret (login + UPLOAD_TOKEN), non si è passati a un controllo basato solo sul ruolo — nessuna modifica al backend.
16. **Login duplicato in `index.html`**: oltre al login "ufficiale" su `hub.html`, `index.html` ha un proprio overlay di login che chiama anch'esso `POST /api/auth/login` e poi reindirizza a `hub.html` dopo aver salvato `_enri_user/_enri_role/_enri_session`. Serve da fallback per chi atterra direttamente su `index.html?direct=1` (link diretto dalla card "Fase 1" in hub.html) senza essere già loggato.
17. **`executive_summary.html` rimosso dal progetto** (confermato dall'utente): non c'è più una card collegata in `hub.html`. Se trovi ancora riferimenti al file in vecchie versioni di documentazione o in altri repo collegati, sono obsoleti.
18. **"Lotti in Progettazione" (ex "Lotti in Avvio")**: i lotti 1–8 non ancora avviati sono ora etichettati "Lotti in Progettazione" ovunque in `index.html` (label visuale, riga totale, modale riepilogo, export). La colonna milestone "Invio Perm./Scadenza" è stata rimossa da quella sezione (ora c'è `milestone.html` dedicata).
19. **`IMPRESE_PER_LOTTO` estesa a tutti i 12 lotti**: in `index.html` l'oggetto include ora anche i lotti 1–8 (`1→Valtellina`, `2→Sertori`, `3→Valtellina`, `4→Sielte`, `5→Circet`, `6→Sertori`, `7→Valtellina`, `8→Sielte`). Il nome impresa compare sotto il numero lotto nelle barre di avanzamento.
20. **SED — bottoni Excel unificati**: la sezione "Attraversamenti SED" in `index.html` aveva due pulsanti separati ("Scarica Vista" / "Scarica Tutti"). Ora è un unico dropdown `sedXlsDropdown` identico a `xlsDropdown` di "Tutte le Pratiche" (con opzioni "Tutti gli attraversamenti" e "Vista corrente").
21. **`debug_dashboard.py`**: script Python di audit automatico per i file HTML della dashboard. Controlla: CSS vars mancanti/undefined, topbar navy, logo, hub button style, div balance, emoji UI vietate vs funzionali Leaflet, gradienti decorativi, backdrop-filter, colori fuori palette, funzioni JS duplicate e dead, variabili JS inutilizzate, DOM ID mancanti, console.log in produzione, segreti hardcoded. Output con score /100 per file. Uso: `python3 debug_dashboard.py file.html [...]`.
22. **Card modal cantieri in `scavi.html` (rev.7)**: `openModalStato()` mostrava solo `pratica_id`/lotto/ente/comune/progresso. Ora mostra tutti i dati disponibili: `codice_cantiere` (primario), cluster, tecnica scavo, impresa, conteggio tratte lavorabili/bloccate (derivato da `r.tratte[]`, **non** da `tratte_lavorabili`/`tratte_bloccate` — quei campi esistono solo nella riga CSV, non nel doc Mongo), le 4 date se valorizzate, motivo blocco+ripresa stimata se `sospeso`, note.
23. ✅ **RISOLTO** — **Palette colori stato fuori brandkit Retelit**: `STATO_COLORS`/`COLORS`/`STATI_COLORI_MAP` usavano hex arbitrari non a palette (viola `#9b59b6`/`#7A3DAA`, rosa `#B5657A`, verde/rosso/blu/arancio non-token). Mappa canonica ora identica su `index.html`, `mappa.html`, `mappa_impresa_caricamento.html`, `scavi.html` (v. §8bis; includeva anche `mappa_impresa.html`, poi rimossa rev.126). **Esclusi deliberatamente**: palette per-lotto/cluster (`LOTTO_COLORS`, rainbow array) e marker misura/ricerca — servono a distinguere entità diverse, non rappresentano uno "stato", restano arbitrari.
24. ✅ **RISOLTO** — **Nesting HTML rotto in `mappa_impresa_caricamento.html`**: `</div>` di troppo chiudeva `#sidebarEl` prima che `#scaviBodyPanel` venisse aperto → il pannello scavi diventava fratello della sidebar invece che figlio, causava "doppia finestra" (mappa schiacciata, legenda sovrapposta al form). Verificare sempre il nesting con `html.parser` prima di assumere che un problema visivo sia CSS quando coinvolge un intero pannello.
25. ✅ **RISOLTO** — **`.pr-form-actions` senza `flex-wrap`**: su sidebar 300px i 3 pulsanti (Annulla/Storico/Salva aggiornamento) andavano in overflow orizzontale e venivano tagliati da `.sidebar{overflow:hidden}` (es. "Annulla" → "nnulla"). Aggiunto `flex-wrap:wrap` in `mappa_impresa_caricamento.html`.
26. ✅ **RISOLTO** — **"Aggiorna Scavi" apriva cantiere sbagliato**: il fallback quando non c'era match esatto sulla tratta prendeva "il primo cantiere dello stesso lotto" alla cieca. Ora: match esatto → apri; nessun match + lotto con 1 solo cantiere → apri; nessun match + lotto con più cantieri → messaggio, nessuna apertura automatica; nessun match + nessun cantiere sul lotto → messaggio "autorizzazione non ancora ottenuta". File: `mappa_impresa_caricamento.html`.
27. ✅ **RISOLTO** — **Bug `LAVORABILE` in `_compute_tratta_summary` (`server.py`)**: `need_no`/`need_ord` venivano letti SOLO dal flag `NULLA OSTA NECESSARIO`/`ORDINANZA NECESSARIA` sulla riga AUT. Se il flag era vuoto/NO ma esistevano comunque righe reali NULLA OSTA/ORDINANZA non ottenute per la tratta, `LAVORABILE` risultava `SI` per errore. Fix: `no_effettivo = (need_no=="SI") or bool(no_latest)` (idem `ord_effettivo`) — vincolante se il flag lo dichiara O se la pratica esiste davvero nei dati.
28. ✅ **RISOLTO** — Integrata la tabella "Tutti i Cantieri" di `scavi.html`: aggiunte colonne `codice_cantiere`, `impresa`, `tecnica_scavo` (label via `TECNICA_LABEL`) — dati già esposti da `GET /api/cantieri` ma non renderizzati. Aggiunti anche a `haystack` di ricerca.
29. ✅ **RISOLTO** — Nuovo endpoint `POST /api/admin/regenerate-derived` (auth `x-upload-token`): rilegge il Master.csv corrente e richiama `_regenerate_derived_files` senza upload — serve a riprocessare i dati esistenti dopo un fix a `_compute_tratta_summary` senza toccare Master.csv.
30. ✅ **RISOLTO** — **Popup mappa: info cantiere integrate** (prima mostravano solo dati pratica/autorizzazione).
    - `mappa_impresa_caricamento.html`: sezione "Cantiere" completa (stato+colore, tecnica, metri scavati/totali+%, impedimento), dati da `SC_CANTIERI` precaricato all'avvio (`scEnsureInit()` fire-and-forget in testa alla IIFE di init mappa).
    - `mappa.html` (già allora anche `mappa_impresa.html`, rimossa rev.126): stessa sezione ma **sola lettura** (nessun bottone azione), dati da `GET /api/cantieri` (pubblico, no auth) in `RO_CANTIERI` (costanti locali `RO_SC_STATO_LABEL/COLOR/TECNICA_LABEL`, funzione `_roLoadCantieri()`).
    - Lookup in tutti e 3 i casi: `cantieri.find(c => (c.tratte||[]).some(t => t.tratta_id === p.TRATTA_ID))`.
31. ✅ **RISOLTO** — `.btn.secondary{color:var(--muted)}` faceva sembrare disabilitati i pulsanti secondari (es. "Nuova pratica"/"Aggiorna Scavi" nel popup mappa apparivano "spenti" accanto al primario blu). Cambiato a `color:var(--text)` + hover `border-color/color:var(--accent)` in tutti e 3 i file mappa.
32. ✅ **RISOLTO** — **`scavi.html`: 3 bug segnalati dall'utente su KPI/barre/tabella (rev.8)**:
    - **KPI cards**: contavano solo i lotti aggregati sul "peggiore" stato, senza metri né %. Ora `_renderKpi()` conta i cantieri reali da `CANTIERI_RAW` e popola `#kpi{Nav,All,Cor,Com,Sos}Sub` con `metri m · pct% sul totale`.
    - **Barre "Avanzamento per Cluster" e "per Lotto"**: la riga scavi era binaria (un unico segmento colorato `in-corso`/stato-peggiore + grigio "non scavato"), non rifletteva i singoli stati dei cantieri che compongono cluster/lotto. Ora `_loadCantieri()` traccia `statoMetri{stato:metri}` per lotto e cluster (propagato in `LOTTI[].statoMetri` e `window._CLUSTER_SCAVI[cl].statoMetri`); `_renderClusters()`/`_renderBars()` disegnano un segmento per ogni stato reale (ordine/colori da `STATO_ORDER`/`STATO_COLOR`) + eventuale segmento grigio "Non tracciato" per i metri senza cantiere associato.
    - **Tabella "Tutti i Cantieri"**: vedi voce 28.
33. ✅ **RISOLTO (rev.9)** — **Bug `metri_totali` incoerente in `_sync_cantieri()` (`server.py`)**: `metri_totali` veniva ricalcolato ad ogni sync sommando solo le tratte con `lavorabile==True` (riga ~2229) e sovrascritto con `$set` senza mai controllare `metri_scavati` già accumulato. Siccome `lavorabile` dipende anche da NULLA OSTA/ORDINANZA (non solo dall'AUTORIZZAZIONE), un cantiere già "in corso" con metri regolarmente rendicontati poteva vedersi azzerare `metri_totali` a un sync successivo (visto in produzione: `CA/3/2A`, 0 totali / 600 scavati). **Chiarito dall'utente**: `lavorabile` è un flag SOLO per la visualizzazione in mappa, non deve limitare cosa un'impresa può rendicontare. Fix: `metri_totali` ora somma **tutte** le tratte della pratica (autorizzazione ottenuta), indipendentemente da `lavorabile` — di fatto uguale a `metri_totali_potenziali` (campo lasciato per compatibilità con pagine che lo leggono separatamente). ⚠️ **Da fare manualmente una tantum**: i documenti `cantieri` già in Mongo restano con il vecchio `metri_totali` finché non gira un sync — chiamare `GET /api/admin/sync-cantieri` (auth `x-upload-token`) per riallinearli subito, altrimenti si autocorreggono al prossimo upload/approvazione di Master.csv.
34. **RISOLTO (rev.21)** — **Tabella "Tutti i Cantieri" in `scavi.html`: header non andava a capo**: `white-space:nowrap` globale su `.cant-table thead th` + `tight:true` che lo riforzava inline → 13 colonne in overflow orizzontale anche con label corte, nonostante gli aggiustamenti di rev.18/rev.20. Fix: `white-space:normal` sugli header (con `line-height:1.35`), `tight` ora `max-width:70px` invece di `nowrap`, `.th-cell` allineato `flex-start` (serve per header su 2 righe), label accorciate (Prov., Stato, Avanz., M. Tot., M. Scavati, Registro). Bottone "Registro" per riga: rimossa emoji 📋 (vietata da brand kit §8.11) e stile inline generico, sostituiti con classe `.btn-registro` (bordo/hover accent Retelit, icona SVG inline al posto dell'emoji).
35. **RISOLTO (rev.22)** — **Card "Registro Lotti · Stato Cantiere" rimossa da `scavi.html`** (ridondante con "Avanzamento per Lotto" + tabella "Tutti i Cantieri"): eliminato il markup della card, la funzione `_renderTabella()` e la sua chiamata in `_renderAll()`. I pill province (`.prov-pill`, stesso stile già usato nella tabella rimossa) sono stati spostati sotto al numero lotto in `_renderBars()` (card "Avanzamento per Lotto"), dentro `.bar-lotto-wrap`.
36. **RISOLTO (rev.23)** — **Colore distintivo per provincia in tutta `scavi.html`**: nuovo `provColor(p)`/`provPill(p)` (hash deterministico su palette di 10 colori brand-coerenti — stessa provincia = stesso colore sempre, non serve enumerare le province a mano). Applicato a: pill "Avanzamento per Lotto", card modal cantiere, colonna "Prov." tabella "Tutti i Cantieri", modal Lotto (era hardcoded su `--accent`), modal Cluster (era testo semplice `MI / PV`), card "Metri scavati per provincia". `.prov-pill` CSS ora senza colori fissi (solo forma), il colore è sempre inline via `provColor()`.
37. **RISOLTO (rev.24)** — **Redesign card cantiere nel modal stato (`openModalStato`)**: erano righe separate da bordo inferiore con testo semplice "Label: valore" — ora card vere (`.msc-*`, bordo+radius+padding), chip invece di label:valore, provincia con `provPill()`, icone SVG al posto delle emoji 📍/⏸/📝 (vietate da brand kit §8.11). ⚠️ Bug corretto nello stesso giro: `background:var(--danger)0d` non è CSS valido (non si concatena testo dopo `var()`) e `--danger` non è nemmeno definita in `scavi.html` — sostituito con hex diretto `#C0392B` (stesso token brand kit). **(rev.25)**: rimossi chip "Tecnica" e "Tratte lavorabili/bloccate" dalla card (giudicati privi di senso in questo contesto dall'utente) — resta solo "Impresa".
38. **Redesign vista dirigenti `scavi.html` (sessione 2026-07-03, no rev.-tag per evitare collisione col rev.38 changelog sotto)**: (a) `.phase-header` ("Stato Cantieri") portato da 9px a 13px, allineato a `.page-title`; (b) **exec-strip** in header: 3 numeri grandi (Avanzamento Fisico %, Permessi Ottenuti %, Cantieri in Sospeso — rosso se >0) — alimentati da `pctGlob`/`pctPermessi`, calcolati in `_renderRiepilogo()` ma prima mai scritti a video (dead code); titolo/eyebrow/sottotitolo pagina rimossi su richiesta utente, resta solo l'exec-strip allineata a destra (`#pageSubDyn` di nuovo assente dal markup, `_renderPageSubtitle()` esce per guard `if(!el)return`, nessun errore); (c) nuova card **"Performance Imprese"** subito sotto i KPI: aggregazione per `impresa` con colonne Lotto/Cluster (multi-valore, Set ordinato con `_lottoCompare`), Cantieri, Completati/In Corso/Sospesi, Metri Tot., % Avanzamento (bar+colore), Ritmo m/gg (da `data_inizio_effettiva`→oggi o `data_fine_effettiva`) — ordinabile per colonna (`_sortImprese()`, funzione `_renderImprese()` in pipeline `_renderAll()`); (d) rimossa legenda duplicata identica tra "Avanzamento per Cluster" e "Avanzamento per Lotto" (resta una sola, con rimando testuale nella seconda). ⚠️ Ritmo m/gg è vuoto per i cantieri senza `data_inizio_effettiva` valorizzata — verificare copertura dato in produzione.
39. **`scavi.html` — bug/rifiniture "Performance Imprese" + modal "Stato Cantieri" (rev.77)**: (a) ✅ **RISOLTO — bug % avanzamento non coerente con la vista "per Lotto"**: `_renderImprese()` calcolava il denominatore sommando `metri_totali` dai soli cantieri già sincronizzati in Mongo per quell'impresa (es. Sielte/lotto 2A = 7852m) invece del totale da PERMESSI (Riepilogo_progettazione.csv, 11.589m per lo stesso lotto — più completo, copre tutte le tratte autorizzate anche se non ancora un cantiere Mongo), come fa correttamente `byLotto`/`LOTTI`. Risultato: stessa `metriScav`, denominatori diversi → 8% mostrato invece di 5,5%. Fix: nuovo `g.metriTotByLotto{}` per-impresa-per-lotto, `metriTotReale` = somma di `PERMESSI[lotto].totale` (fallback sul dato cantieri se il lotto non è in PERMESSI) — usato sia per il denominatore di `pct` sia per la colonna "Metri Tot." visualizzata (prima disallineata rispetto al nuovo pct). (b) ✅ **RISOLTO** — colonne Lotto/Cluster della tabella non erano ordinabili: mancavano `class="imp-th-sort"`+`onclick="_sortImprese(...)"` sui `<th>`, e il comparator confrontava direttamente due `Set` JS (`a.lotti < b.lotti` è sempre `false` → nessun ordinamento) — ora le due chiavi vengono convertite alla stessa stringa ordinata mostrata a video (`[...set].sort(...).join(', ')`) prima del confronto. Ordinamento di default per "miglior avanzamento" (`{key:'pct', dir:'desc'}`) era già corretto, il sort rotto sulle altre colonne dava probabilmente l'impressione che l'intera tabella non si riordinasse. (c) Rinominata label header "Avanzamento Fisico" → "Avanzamento globale" (solo testo, chiave dato `execPctScavi` invariata). (d) Card "Avanzamento per Cluster": rimosso il nome descrittivo del cluster (es. "Milano urbano", da `CLUSTER_LABEL`) — resta solo il badge "Cluster N"; `CLUSTER_LABEL` lasciata invariata altrove (modal cluster, dove il nome resta utile). (e) `openModalStato()` (modal aperta cliccando le card KPI "Stato Cantieri"): sostituita la lista a card `.msc-*` con la stessa tabella/colonne di "Tutti i Cantieri" (Cod. Cantiere, Pratica, Ente, Lotto, Cluster, Prov., Comune, **Impresa**, Stato, Avanz., M. Tot., M. Scavati, bottone Registro) — chiude anche TODO §11 12.1/12.2 (impresa ora visibile sia in "Performance Imprese" sia nelle card modal cantiere). ⚠️ **Trade-off non richiesto esplicitamente**: la vecchia card mostrava anche `motivo_blocco`/`data_ripresa_stimata` (solo per stato Sospeso) e `note` libere, assenti nella tabella "Tutti i Cantieri" — questi due dati non sono più visibili da questa modal. Segnalato all'utente, non reintegrato salvo richiesta. (f) ✅ **RISOLTO (rev.78, follow-up immediato)** — dopo il punto (e) il div `.modal` di `#modalStatoOverlay` era rimasto a `width:560px` inline (dimensionato per la vecchia lista card), causando scroll orizzontale forzato con la tabella a 13 colonne (screenshot utente: solo le prime 7 colonne visibili). Portato a `width:1180px;max-width:96vw` — stesso pattern già usato per il modal Cluster di `index.html` in rev.75 quando gli fu aggiunta una colonna.
40. **Formato reale di `Master.csv`**: il file di produzione è **tab-separated** (non comma, non semicolon) — `server.py` lo scrive sempre con `sep="\t"` verso GitHub/derivati. Qualsiasi `Master.csv` ricevuto per editing va verificato con `head -1 file | cat -A` prima di assumerne la struttura: se ogni riga appare come un unico blob tra virgolette, è corrotto (visto in rev.50, archiviato) e va riconvertito prima di qualunque modifica. Occhio anche a varianti `;`-separated/latin-1/CRLF (viste in rev.63-64, archiviato) e a nomi colonna quasi-identici (`DATA_PREVISTO_RILASCIO` vs `DATA_PREVISTA_RILASCIO`, femminile è quello corretto usato dal codice).

41. **Split tratte esistenti (nuovi TRATTA_ID) — sessione 2026-07-06**: `_regenerate_derived_files` patcha SOLO le property delle feature già presenti in `QGIS.geojson` (match per TRATTA_ID, geometria invariata) — non crea geometrie nuove. Procedura corretta: 1) split linea in QGIS, nuovi TRATTA_ID univoci, export geojson (verificato CRS export = EPSG:4326/CRS84, non UTM, altrimenti coordinate fuori scala e mappa mostra vista mondo); 2) upload con `target=QGIS.geojson` esatto — `admin.html` valida che il nome file scelto combaci col target selezionato (riga ~842), quindi niente più rischio di salvataggio sotto nome sbagliato; 3) SOLO DOPO caricare/approvare Master.csv coi nuovi TRATTA_ID. Ordine invertito = righe orfane senza geometria. ⚠️ **Rischio residuo non risolto**: nessun lock/versioning ottimistico tra upload manuale admin e `_regenerate_derived_files` triggerato da approvazione `pending_updates` impresa — se un'approvazione impresa scatta a ridosso dell'upload manuale del geojson, vince chi scrive per ultimo su `uploaded_at`. Dopo un upload critico, verificare sempre `GET /api/files` (campo `size`/`modified` dell'entry) per confermare che sia la versione servita, prima di considerare chiuso il lavoro.

42. **RISOLTO (2026-07-09)** — **`imprese.html`, tab "Le mie submission": aggiunto codice pratica + fix bug "Nessun campo" sulle submission "Nuova pratica"**. Contesto: ogni `change` di una submission `update` ha forma `{tratta_id, ente, tipo_permesso, original_pratica, lunghezza, fields:{...}}`, mentre una submission `new` ha `change` = record flat completo (`Source.Name`, `TRATTA_ID`, `TIPO_PERMESSO`, `PRATICA`, ecc., **senza** wrapper `fields`). `showSubDetail()` leggeva sempre `c.fields||{}` → per le submission "Nuova pratica" risultava sempre `{}`, quindi il popup mostrava "Nessun campo" nonostante i dati fossero presenti nel `change` stesso. Fix:
    - Nuovo helper `_codiceForChange(c, type)`: per `type==='new'` usa `buildCodice(c)` diretto (il change ha già tutti i campi); per `type==='update'` cerca in `PRATICHE` (già caricato) la riga con `TRATTA_ID`+`ENTE`+`TIPO_PERMESSO` (+ `original_pratica` se disponibile) per recuperare `_codice` (serve il lotto/`Source.Name`, assente nel change di update).
    - Tabella "Le mie submission": nuova colonna "Codice pratica" (primo codice + `+N` se la submission raggruppa più pratiche in un solo invio).
    - Popup dettaglio: ogni blocco `.sub-change` mostra ora il codice pratica accanto a tratta/ente/tipo; header del popup mostra la lista di tutti i codici coinvolti se >1; per `type==='new'` i campi mostrati ora sono l'intero record (esclusi i campi già in header: `Source.Name`/`TRATTA_ID`/`ENTE`/`TIPO_PERMESSO`) invece di un oggetto vuoto.
    - Sequenziato `loadPratiche()` prima di `loadMine()` nel boot (`await`) — prima partivano in parallelo, rischio di race condition sul lookup `_codiceForChange` per le submission `update` se `PRATICHE` non era ancora popolato al primo render.

⚠️ **Segnalato, non risolto**: font-size sotto i 12px in decine di punti di `scavi.html` (9px/10px/10.5px/11px su badge, chip, celle tabella, eyebrow) — viola la regola brand kit §4.6 "mai sotto 12px in interfaccia". Font-family/pesi (Raleway + JetBrains Mono) invece corretti e caricati bene. In attesa di conferma utente prima di un pass esteso (60+ punti, rischio di rompere il layout di badge/tabelle compatte).

43. **RISOLTO (2026-07-09)** — **`imprese.html`, tab Solleciti: `PROTOCOLLATO INTEGRAZIONE` escluso per errore dalla lista pratiche sollecitabili**. `SOL_STATI_ESCLUSI` lo trattava come uno stato "chiuso" (insieme a `NECESSARIA INTEGRAZIONE`/`IN REDAZIONE INTEGRAZIONE`), ma in `STATO_TRANSITIONS` ha lo stesso ruolo di `PROTOCOLLATO`: pratica (di integrazione) inviata all'ente, in attesa di risposta (→ `NECESSARIA INTEGRAZIONE` o `OTTENUTO`) — quindi va sollecitato esattamente come `PROTOCOLLATO`. Rimosso dall'esclusione. Effetto visibile: prima la lista "Nuovo sollecito" mostrava quasi solo pratiche `INVIATO`/`PROTOCOLLATO` (segnalato dall'utente via screenshot), ora include anche le integrazioni protocollate.
⚠️ Non verificato in questo giro: logica del numero sollecito automatico (`sol-numero`).


---

## 8bis. Palette colori stato — mapping canonico (tutte le pagine)

Usata da `STATO_COLORS` (mappa/scavi) e `COLORS`/`STATI_COLORI_MAP` (index). Se aggiungi/tocchi uno di questi dizionari in una pagina, allinealo a questi valori:

| Stato | Hex | Token brandkit |
|---|---|---|
| IN ATTESA / IN REDAZIONE | `#6B7685` | `--gray-500` |
| IN FIRMA RDS | `#D08A1A` | `--warn` |
| INVIO PRELIMINARE | `#043F75` | `--retelit-blue` |
| INVIATO / PROTOCOLLATO | `#41BBD9` | `--retelit-sky` |
| NECESSARIA INTEGRAZIONE | `#C0392B` | `--err` |
| IN REDAZIONE INTEGRAZIONE / PROTOCOLLATO INTEGRAZIONE | `#436A93` | `--retelit-blue-75` |
| OTTENUTO | `#1E9E6A` | `--ok` |

File allineati: `index.html`, `mappa.html`, `mappa_impresa_caricamento.html`, `scavi.html`, `imprese.html`.

**2026-07-09** — fix colori stato (bug, non allineamento di routine):
- `mappa_impresa_caricamento.html`: `STATO_COLORS['IN REDAZIONE INTEGRAZIONE']` era `#C0392B` (rosso, sbagliato) invece di `#436A93` (blu) → corretto.
- `imprese.html`: non aveva alcuna `STATO_COLORS`. Le celle stato usavano `class="stato-${stato.replace(/\s+/g,'.')}"` con regole CSS tipo `.stato-IN.REDAZIONE` — selettore composto che richiede DUE classi separate (`stato-IN` AND `REDAZIONE`), non matcha mai un'unica classe col punto letterale generata dal replace. Risultato: tutti gli stati multi-parola (`NECESSARIA INTEGRAZIONE`, `PROTOCOLLATO INTEGRAZIONE`, `IN REDAZIONE INTEGRAZIONE`) non prendevano MAI il colore dal CSS. Fix: aggiunta `STATO_COLORS` locale (identica alla tabella sopra) + `color` inline via helper `statoColor()`, rimosse le classi `.stato-X` rotte. Stesso fix di pattern applicato alla card pratiche sidebar in `mappa_impresa_caricamento.html` (usava lo stesso selettore rotto nonostante avesse già `STATO_COLORS`/`getColor()` disponibili — ora usa `getColor()` inline).
- Verificato: `scavi.html`, `mappa.html`, `hub.html` non usano questo pattern — bug isolato a queste due pagine.

---

## 9. Modello dati MongoDB

```jsonc
// collection: uploads (storico versioni file)
// source: "impresa" (Master.csv da approvazione) | "derived" (QGIS.geojson/Riepilogo
// rigenerati automaticamente) | assente per upload manuali da admin.html
{
  "_id": ObjectId,
  "filename": "Master.csv",            // path relativo a DATA_DIR
  "original_name": "Master_v2.xlsx",
  "size": 192167,
  "content_type": "text/csv",
  "project": "main",                   // "main" | "M" | "pm"
  "rows": 3120,
  "uploaded_at": "2026-06-23T11:30:00+00:00",
  "gridfs_id": ObjectId,               // → fs.files (None se cancellato)
  "deleted_at": null | "ISO",
  "source": "impresa" | "derived" | null,
  "note": "Submission <id> from <nome> (update)" | "restore/delete Master.csv" | null
}

// collection: assignments (impresa → lotti)
{
  "_id": ObjectId,
  "nome": "Costruzioni Alfa Srl",
  "lotti": ["Lotto 1", "Lotto 1A"],
  "active": true,
  "created_at": "ISO",
  "updated_at": "ISO"
}

// collection: pending_updates (workflow approvazione)
{
  "_id": ObjectId,
  "nome": "Costruzioni Alfa Srl",
  "type": "update" | "new",
  "changes": [
    // type=update
    {"tratta_id": "TR_0103", "ente": "...", "tipo_permesso": "AUTORIZZAZIONE",
     "fields": {"STATO_PERMESSO": "OTTENUTO", ...}},
    // type=new
    {"TRATTA_ID": "...", "ENTE": "...", ...}
  ],
  "status": "pending" | "approved" | "rejected",
  "submitted_at": "ISO",
  "reviewed_at": "ISO" | null,
  "reviewed_by": null,
  "applied_upload_id": "<id Master.csv versione generata>" | null,
  "summary": {"updated": 3, "added": 0, "not_found": 0} | null,
  "reviewed_note": "motivo rifiuto" | null,
  "note": "nota libera dell'impresa"
}
```

File content in `fs.files` / `fs.chunks` (motor `AsyncIOMotorGridFSBucket`, bucket `files`).

```jsonc
// collection: cantieri (NEW — un documento per pratica di autorizzazione)
{
  "_id": ObjectId,
  "cantiere_key": "24|1A",          // num pratica | lotto — NON pratica_id (non univoco tra enti)
  "codice_cantiere": "CA/3/1A",     // NEW — identificativo cantiere mostrato in UI, distinto da pratica_id
  "codice_progressivo": 3,          // NEW — contatore progressivo per lotto, assegnato una sola volta alla creazione
  "pratica_id": "AUT/24/1A",        // resta come riferimento pratica, non più usato come nome del cantiere in UI
  "lotto": "1A",
  "cluster": "...",
  "ente": "...",
  "stato_cantiere": "...",          // valori in STATO_CANTIERE_VALUES
  "tecnica_scavo": "...",           // valori in TECNICA_SCAVO_VALUES
  "data_inizio_prevista": "ISO" | null,
  "data_inizio_effettiva": "ISO" | null,
  "data_fine_prevista": "ISO" | null,
  "data_fine_effettiva": "ISO" | null,
  "metri_scavati": 0,                // accumulato via $inc da metri_realizzati_oggi
  "note": "...",
  "motivo_blocco": "...",
  "data_ripresa_stimata": "ISO" | null,
  "impresa": "Costruzioni Alfa Srl",
  "updated_at": "ISO",
  "log": [ {"data": "AAAA-MM-GG", "impresa": "...", "stato_cantiere": "...",
            "metri_realizzati": 0, "note": "...", "motivo_blocco": "...",
            "data_ripresa_stimata": "..."} ]
}

// collection: sopralluoghi (NEW)
{
  "_id": ObjectId,
  "codice_verbale": "VBS-2026-0042",
  // + campi del form (cantiere, segnalazioni, azioni richieste, firme, riferimenti foto su GitHub)
}

// collection: solleciti (NEW)
{
  "_id": ObjectId,
  "tratta_id": "...",
  "pratica": "...",
  "impresa": "Costruzioni Alfa Srl",
  "tipo_sollecito": "...",
  "created_at": "ISO"
}

// collection: pol_conv_dates (NEW — chiave: "{lotto}|{pratica}|CONVENZIONE|POLIZZA")
{
  "_id": "1A|11|CONVENZIONE",
  "data_richiesta": "AAAA-MM-GG"   // fissata una sola volta con $setOnInsert
}

// collection: access_logs (NEW — ex JSONBin, un documento per binId)
{
  "_id": "69c8fbdced015c742bc8e978",   // il vecchio LOG_BIN_ID, riusato come chiave Mongo
  "utenti":  ["Mario Rossi", "Costruzioni Alfa Srl", "..."],
  "accessi": [ {"ts":"ISO","utente":"...","ruolo":"...","ip":"...","ua":"...",
                "durata":0,"lotti":[],"nAperture":0,"pagina":"scavi"} ]
}
```

---

## 10. Flusso impresa end-to-end (riepilogo)

1. Admin → `admin.html` tab "Assegnazioni imprese" → aggiunge `Costruzioni Alfa Srl` con lotti `Lotto 1, Lotto 1A`.
2. L'impresa accede su `hub.html` con nome esattamente `Costruzioni Alfa Srl` + codice → backend verifica con Apps Script → restituisce session token (`_enri_session`).
3. Hub entra in "modalità impresa" (check `/api/imprese/me`) e mostra fino a 4 card: Aggiorna Pratiche, Mappa (sola vista), Mappa con aggiornamento, Avanzamento Scavi.
4a. **Flusso pratiche** (con approvazione): Click → `imprese.html` o `mappa_impresa_caricamento.html` → coda modifiche → "Invia per approvazione" → Admin tab "Coda imprese" → Approva → Backend applica `changes` a Master.csv, crea nuova versione GridFS, rigenera QGIS+Riepilogo, **pusha tutti e tre su GitHub** → `_sync_cantieri()` riallinea i cantieri.
4b. **Flusso scavi** (scrittura diretta, NEW): Click → `imprese_scavi.html` → aggiorna stato cantiere/metri giornalieri → `POST /api/imprese/cantieri/{key}` scrive subito su MongoDB e pusha su GitHub, **senza passare da `pending_updates`**.
5. Dashboard (`/api/data/Master.csv`, `/api/cantieri`) serve immediatamente la nuova versione; SWR sulle pagine aggiorna la UI live senza reload.

## 11. TODO aperti (da Checklist Bug e Migliorie, importata 2026-07-02)

**Regola permanente**: dopo ogni modifica (in questa sessione o future), verificare se corrisponde a una voce di questa tabella e aggiornarne subito lo Stato (es. "Chiuso (rev.NN)" + breve nota), anche se non era quella la richiesta esplicita dell'utente.

Fonte: checklist Excel utente. Solo voci non "Completato" (34/59 già completate, non riportate qui — vedi file originale per lo storico completo).

| ID | Pagina | Descrizione | Stato | Priorità | Note |
|---|---|---|---|---|---|
| 6.1 | imprese_scavi | Implementare modello strutturato caricamento dati scavi | In corso | Alta | |
| 6.3 | imprese_scavi | Identificare informazioni da visualizzare nella pagina dedicata | In corso | Alta | |
| 6.4 | imprese_scavi | Migliorare la visibilità delle card dei cantieri | In corso | Alta | |
| 10 | NEW | Pagina Gantt progetto + tabella associazione impresa/lotti/cluster (%design, %perm, %delivery, metri totali) | Da fare | Alta | ENTRO MERCOLEDÌ |
| 11.1 | Index | Eliminare previsione "mia" e richiederla all'impresa | Chiuso (rev.48) | Alta | Fatto ovunque: `index.html`, `Master.csv` (colonna `DATA_PREVISTA_RILASCIO`), `server.py` (nessuna whitelist campi, generico — vedi nota sotto), `imprese.html`, `mappa_impresa_caricamento.html`. Campo previsione nascosto/non richiesto quando stato→OTTENUTO (non ha senso chiederla se la pratica è appena stata ottenuta). Presente anche nel form "nuova pratica" di entrambe le pagine impresa. |
| 11.2 | Index | Creare nuova colonna "update" | Già presente (verificato rev.44) | Alta | Colonna `Update` già implementata nelle tabelle pratiche (`_mkTh('Update','update',...)`, case `'update'` in `_colGetDisplay`/`_colGetSort`) |
| — | All | Rename `COORDINAMENTO`→`INVIO PRELIMINARE` | Risolto (rev.43) | — | Verificato file per file con grep mirato dopo lo scarto delle voci fabbricate (rev.39): Master.csv/server.py già ok, scavi.html e mappa_impresa.html corretti in rev.43, mappa.html/mappa_impresa_caricamento.html già ok |
| 11.4 | Index | Portare i solleciti nei popup insieme alle note | Chiuso (verificato rev.167) | Alta | Solleciti visibili in `index.html` via badge dedicati: `_solBadge`/`solPopoverBox` per pratica e `_solSedBadge`/`solSedPopoverBox` per SED (dati da `solleciti.csv`, riga ~5841). Popup separato dal popup Note (non un unico box fuso) — implementazione preesistente, non individuata nel check superficiale precedente |
| 11.6 | Master | Pratica NO/27/1A: correggere data invio (da quando è partito ENRI) | Da fare | Alta | |
| 11.9 | Master | % ottenuto Valtellina non compare (lotto piccolo?) | Risolto (rev.49) | Alta | Non era un problema di Valtellina: soglia `pct>=5` nascondeva del tutto la % nelle barre modal lotto/cluster quando il segmento era piccolo (es. OTTENUTO con 1 sola pratica) |
| 12.1 | Scavi | Associazione visibile lotto/impresa | Chiuso (rev.77) | Alta | Coperta in "Performance Imprese" (colonne Lotto/Cluster) e ora anche nelle card modal cantiere (Stato Cantieri), che mostrano la tabella completa con colonna Impresa (v. §4.39) |
| 12.2 | Scavi | Rendere più chiara nelle card stato l'impresa associata al cantiere | Chiuso (rev.77) | Alta | Modal "Stato Cantieri" ora usa la stessa tabella di "Tutti i Cantieri", con colonna Impresa dedicata (v. §4.39) |
| 12.3 | Scavi | Stato cantieri in topbar più grande | Chiuso (rev.76) | Alta | `.phase-header` 9px→13px, allineato a `.page-title` |
| 12.4 | Scavi | Prima della tabella cluster, tabella imprese con tutte le info associate | Chiuso (rev.76) | Alta | Nuova card "Performance Imprese" subito sotto i KPI, sopra "Avanzamento per Cluster" |
| 13 | Scavi | Rendere la pagina adatta a vista dirigenziale/alto livello | Chiuso (rev.76) | — | exec-strip KPI + card Performance Imprese + rimossa legenda duplicata |
| — | Scavi | Font-size sotto i 12px (badge/chip/celle/eyebrow) fuori brand kit §4.6 | Chiuso (verificato rev.101) | Media | Grep mirato su tutto `scavi.html`: nessun `font-size` sotto 12px residuo — risolto in una revisione precedente non tracciata singolarmente qui |
| — | Mappa_imprese_caricamento | Verificare export geojson dei lotti per le imprese | Chiuso (verificato rev.136) | Bassa | `exportGeoJSONLotti()` presente e funzionante da rev.78-80 (SED custom modal, `EXPORT_EXCLUDE_PROPS` filtra i campi gestionali) |
| — | Imprese | Manca stato ex-coordinamento (Invio Preliminare) da integrare dopo "In Redazione" | Chiuso (verificato rev.112) | — | Già presente in `imprese.html` `STATO_TRANSITIONS` (IN FIRMA RDS→INVIO PRELIMINARE→INVIATO, caso speciale AUTORIZZAZIONE+Comune di Milano) |
| — | Master/server | Colonna `DATA_UPDATE` manca in `Master.csv` | Chiuso (rev.114, verificato rev.115 su `server.py`) | Media | Colonna presente, gestita backend: righe 1469-1491 (stampata se assente in `fields`, sia su update esistente sia su nuova riga), più `_touch_data_update`/`_touch_data_update_multi` (righe 1935-1975) per i solleciti. Nota: usa `datetime.now(timezone.utc)`, non locale — `DATA_RICHIESTA`/`DATA_APPROVAZIONE` invece arrivano dal frontend (input utente), non generate server-side, quindi nessun disallineamento reale tra i due |
| — | milestone.html / admin.html | Pagine senza guardia auth completa | Chiuso (rev.131-132) | Alta (sicurezza) | Guardia client-side aggiunta a entrambe (redirect `hub.html` se ruolo non consentito). Resta solo client-side, non un vero controllo server — bypassabile settando `localStorage` a mano; per le pagine impresa la vera barriera è `_require_staff_session` lato server (rev.129) |
| — | server.py | Endpoint non-impresa senza controllo ruolo (solo token valido) | Chiuso (rev.129) | Alta (sicurezza) | `_require_staff_session` (403 su ruolo `impresa`) su `lotti-cantieri`, `sopralluoghi`×3, `cantieri`, `cantieri/{key}/log`. Verifica pendente rev.129/130 chiusa in rev.136: `mappa.html` non chiama questi 2 endpoint; `sopralluoghi.html` già inviava `x-session-token` su tutte e 4 le fetch |
| — | server.py | GridFS senza limite versioni, crescita illimitata | Chiuso (rev.130-131bis) | Media | `KEEP_VERSIONS=4` + `_prune_old_versions` su ogni insert (upload/derived/master); endpoint one-shot `POST /api/admin/prune-versions` per l'arretrato pre-esistente |
| — | server.py | Login senza rate limit (bruteforce codice impresa) | Chiuso (sessione 2026-07-06) | Media | In-memory per `nome` lowercased: 5 tentativi falliti → lockout 429 per 300s (`LOGIN_MAX_ATTEMPTS`/`LOGIN_LOCKOUT_SECONDS`), reset su login riuscito. Non persiste tra restart/istanze multiple (accettabile per minaccia "smanettone", non per attacco distribuito) |
| — | server.py | `SESSION_SECRET` a 8 cifre numeriche | Aperto (segnalato, non affrontato su scelta utente) | Bassa (voluta) | Bruteforce offline HMAC in tempi brevi se un token viene intercettato. Utente ha valutato il rischio accettabile per la minaccia target ("smanettone", non attaccante dedicato) |

---

_Ultimo aggiornamento: 2026-07-16 (rev. 189) — rev.189: **nuovo ruolo `dl`** (Direzione Lavori) — richiesta utente: gli account DL devono vedere tutto come `user` tranne la pagina Milestone di Progetto. `hub.html`: `SCAVI_ALLOWED_ROLES` include ora `dl` (card Scavi sbloccata); nuovo `id="milestoneCard"` sulla card Milestone, nascosta (`display:none`, href rimosso) se `ruolo==='dl'`. `gantt.html`: guardia estesa a `dl` (era `['admin','admin2','user']`) per coerenza — `user` può già raggiungere la pagina via URL diretto anche se la card in hub resta visibile solo agli admin. `milestone.html` **non toccato lato guardia** (resta `['admin','admin2','user']`, `dl` esclusa). Su richiesta esplicita dell'utente ("non puoi modificare server per il controllo?") il blocco è stato reso reale lato server, non solo client-side: i dati (`IMPRESE_ROWS`/`CONTRACT`, prima hardcoded nello script della pagina) sono stati spostati in `backend/server.py` (`MILESTONE_IMPRESE_ROWS`/`MILESTONE_CONTRACT_ROWS`) dietro nuovo endpoint `GET /api/milestone` e nuova dependency `_require_milestone_session` (= `_require_staff_session` + 403 se `ruolo=='dl'`). Il ruolo è letto dal token di sessione firmato HMAC (`_verify_session`), non falsificabile lato client forzando `localStorage` — a differenza del redirect client-side puro già in uso su `admin.html`/`milestone.html` (guardia UI, non un vero controllo). `milestone.html`: rimossi gli array hardcoded, sostituiti da `_loadMilestoneData()` (fetch `/api/milestone` con header `x-session-token`, redirect a `hub.html` su 403, messaggio d'errore inline su altri errori); `GANTT_ROWS` ora derivato a runtime da `_buildGanttRows()` a partire dagli stessi due array ricevuti dal backend (nessuna duplicazione dati), invece di essere una terza copia hardcoded separata. `py_compile`/`node --check` OK su tutti i file toccati.\n_Ultimo aggiornamento: 2026-07-15 (rev. 188) — rev.188: **server.py + index.html + admin.html** — richiesta utente: distinguere in `index.html` (popup "Note") e `admin.html` (tab Note pratiche) se una nota su una pratica è stata scritta da un Admin (etichetta "Retelit", qualunque admin) o da un'impresa (etichetta "Impresa", qualunque impresa) — prima indistinguibili (`NOTE` è una singola colonna testo senza campo autore). Fix: nuovo helper `_tag_note(note, tag)` (regex `_NOTE_TAG_RE`) prefissa il testo con `[RETELIT] `/`[IMPRESA] ` prima di scriverlo in Master.csv — applicato in `add_admin_note` (tag RETELIT) e in `impresa_submit` (tag IMPRESA, sia `type=update` che `type=new`, unico choke point per `imprese.html`+`mappa_impresa_caricamento.html`). `_tag_note` rimuove un tag preesistente prima di riapplicarlo, per evitare prefissi impilati se un admin modifica una submission impresa via `PUT /api/admin/pending-updates/{id}`. Frontend: `index.html` (popup Note, riga ~6046) e `admin.html` (risultati ricerca note, riga ~1466) parsano il prefisso con la stessa regex, mostrano un chip colorato ("Retelit"=`--retelit-blue`, "Impresa"=`--retelit-teal`) e nascondono il prefisso grezzo dal testo. Note storiche già presenti in Master.csv (scritte prima di questa modifica) restano senza tag/chip, nessun backfill fatto. ⚠️ **Non gestito**: se un admin aggiunge un campo `NOTE` ex-novo editando il JSON grezzo di una submission pending (stesso endpoint sopra), quella nota resta senza tag (il tagging avviene solo alla creazione in `impresa_submit`, non alla modifica admin) — edge case raro, segnalato non risolto. `py_compile`/`node --check` OK.
_Ultimo aggiornamento: 2026-07-13 (rev. 187) — rev.187: `gantt.html`, correzione al fix di rev.186 (segnalato dall'utente con screenshot: RHO AUT/23/1A · 0,22km e PERO AUT/13/1A · 0,22km ancora a 0% nonostante Ottenimento permesso mostrasse 78%/89% reali). **Causa reale**: non era la logica NO COMPETENZA/everAdvanced (verificata corretta contro Master.csv reale, `everAdvanced=true` per entrambe), ma il fatto che una stessa pratica può essere spezzata in **più tratte fisiche separate** nel Gantt — es. AUT/13/1A compare sia come segmento da 1,75 km (dentro il gruppo COMUNE DI PERO) sia come segmento da 0,22 km (dentro il gruppo COMUNE DI RHO, dove la tratta attraversa il confine comunale) — stesso `_pratica`+`_ente`, due `bar` "Progettazione" distinte in `GANTT_ROWS`. Il fix di rev.186 usava `GANTT_ROWS.find(...)` per individuare la riga Progettazione da aggiornare: si ferma al primo match, quindi il primo segmento veniva aggiornato (due volte, ridondante, una per ogni riga Ottenimento del gruppo) mentre il secondo restava sempre al baseline statico 0.0. Fix: `find` → `filter(...).forEach(...)`, aggiorna tutti i segmenti con lo stesso `_pratica`+`_ente`, non solo il primo. Verificato: 4 occorrenze `_pratica:"13"/_ente:"COMUNE DI PERO"` e 4 di `_pratica:"23"/_ente:"COMUNE DI RHO"` in `GANTT_ROWS` (2 righe × 2 segmenti ciascuna), ora tutte aggiornate. `node --check` OK.

_Ultimo aggiornamento: 2026-07-13 (rev. 186) — rev.186 (continua, stessa sessione): `gantt.html`, richiesta utente: "se le date iniziano da aprile non ha senso tenere i mesi di gennaio, marzo ecc." — l'header mensile del gantt aveva `T0` fisso a `01/02/2026` e l'array `months`/`mLabels` hardcoded FEB→GEN, quindi mostrava sempre mesi vuoti prima dell'inizio reale dei dati (baseline task partono 29/04/2026). Fix: `T0` ora calcolato dinamicamente come il mese della data più antica realmente presente in `GANTT_ROWS` (`bar.start`/`ms.date`, fallback 01/02/2026 se nessuna data valida), e l'array `months`/`mLabels` generato con un loop da `T0` a gennaio 2027 (invece dell'array fisso a 12 mesi). Si adatta automaticamente se in futuro le date di baseline cambiano, senza dover più toccare l'array a mano. `node --check` OK.

_Ultimo aggiornamento: 2026-07-13 (rev. 186) — rev.186: `gantt.html`, 2 richieste utente (screenshot: ANAS NO/11 e NO/22 con "Progettazione e presentazione permesso" bloccata a 0% mentre pratiche vicine erano al 100%). (1) **Bug reale trovato in `mergeMasterCsv()`**, regressione introdotta dalla stessa logica di rev.180: quando l'ultimo stato Master.csv di una pratica (per `DATA_ULTIMA_MODIFICA`) è `NO COMPETENZA`, `statoToPct()` ritorna `null` e la funzione usciva (`return`) **prima** di valutare la riga Progettazione — anche se la pratica era realmente passata da IN FIRMA RDS/INVIATO prima di chiudersi come NO COMPETENZA (caso reale ANAS 11 e 22, confermato in Master.csv). Fix: nuova mappa `everAdvanced[pratica|ente]` costruita scorrendo **tutte** le righe storiche (non solo l'ultima per data) — true se la pratica è mai passata oltre IN ATTESA/IN REDAZIONE. La riga Progettazione ora usa `everAdvanced[matchKey]` invece del solo ultimo stato; il blocco `if (pct === null) return` non esce più a monte, permesso e progettazione sono valutati indipendentemente. Verificato su Master.csv reale: pratica 11 (INVIATO→NO COMPETENZA) e 22 (idem) ora → Progettazione 100%; pratica 26 EST VILLORESI (nessun NO COMPETENZA) invariata a 100%. `node --check` OK. (2) **Nuova feature**: barra di riepilogo (rollup, stile MS Project) sulle righe gruppo `g`/`g2` quando collassate — prima il collasso mostrava solo testo "(N task nascosti)", senza indicazione visiva della finestra temporale/avanzamento del contenuto nascosto. Nuova `_groupRollup(groupId)` (accanto a `_groupRowCount`, stesso pattern di scan tra bordi di gruppo): calcola min(start)/max(end) su tutte le barre+milestone nascoste e pct medio delle sole barre. Nuovo `xPosDate(d)` (variante di `xPos` che accetta un `Date` invece di una stringa gg/mm/aaaa) + `fmtDate(d)` per il tooltip. Disegnata come rect arrotondato semi-trasparente (outline+fill proporzionale al pct), h=8 su riga `g`/h=7 su riga `g2`, con `data-tip` (tooltip hover) e `data-group-id` (**mantiene cliccabile il toggle espandi/collassa** anche sopra la barra — il click handler esistente legge `data-group-id` da `e.target`). Nessuna modifica a `COLLAPSED_GROUPS`/logica di collasso esistente, solo rendering aggiuntivo quando `topCollapsed`/`subCollapsed` sono true. `node --check` OK.

_Ultimo aggiornamento: 2026-07-10 (rev. 185) — rev.185: `gantt.html`, bug reale segnalato dall'utente con screenshot (AIPO/RFI, gap di settimane tra Ottenimento permesso e Opere Civili) — **non era quello il bug** (la sequenza Ottenimento→Opere Civili risultava corretta, propagateGanttDatesFromPermits già la garantiva), ma verificando a fondo è emerso un bug reale un passo più avanti nella catena.

**Bug trovato**: da rev.182, Opere Civili viene ricalcolata a metri/giorno (non più dalla baseline uniforme del `.mpp`), ma Test e collaudo/As Built restavano ancorate alle vecchie date fisse del `.mpp` (pensate per la vecchia durata uniforme ~34gg) — per pratiche piccole (es. AIPO 0,05km → 1gg di Opere Civili invece di 34), si apriva un gap di **35 giorni** tra la fine di Opere Civili e l'inizio di Test e collaudo. Confermato riproducendo lo scenario esatto dello screenshot in Node/Playwright (non solo a occhio sul pixel): `Opere Civili end=22/07 → Test e collaudo start=26/08` invece di 23/07.

**Fix**: nuova `cascadeOpereCiviliDurations()` — per ogni Opere Civili calcola lo scostamento tra la nuova fine (ricalcolata a metri) e quella ORIGINALE del `.mpp` (nuovo campo `_mppBaseEnd`, immutabile, catturato subito dopo l'assegnazione id, prima di qualunque ricalcolo), e lo trasla sui successori (Test e collaudo, As Built) con lo stesso meccanismo già usato per i permessi. Chiamata sia dopo la prima passata sincrona di default sia dopo `applyGanttRates()` (tassi risolti).

**Secondo bug trovato durante la verifica incrociata** (non nello screenshot, ma nello scenario combinato permesso-spostato + km-non-uniformi): le due cascate — quella da spostamento permesso (`propagateGanttDatesFromPermits`) e quella da ricalcolo durata (`cascadeOpereCiviliDurations`) — si sarebbero **sovrascritte a vicenda** invece di sommarsi, perché entrambe scrivevano `row.start`/`row.end` direttamente dalla stessa baseline pre-esistente. Refactor: `_cascadeDeltaFromRow()` ora accumula lo scostamento in due campi separati (`_deltaPermesso`, `_deltaKm`) e li **somma** prima di applicarli a `_baseStart`/`_baseEnd` (via `_applyAccumulatedDeltaToRow`), invece di scrivere la data finale direttamente — così una riga può ricevere entrambi gli scostamenti contemporaneamente senza che l'ultimo che gira cancelli l'altro. `propagateGanttDatesFromPermits()` e `cascadeOpereCiviliDurations()` condividono ora lo stesso helper, deduplicando la logica di cascata (prima duplicata).

Testato: riproduzione esatta dello screenshot (fix confermato, gap 22/07→26/08 diventato 22/07→23/07); scenario combinato permesso +25gg e durata km -34gg sulla stessa pratica (risultato netto corretto, verificato `_deltaKm`/`_deltaPermesso` entrambi presenti e sommati); scansione automatica su tutte le 29 pratiche uniche del lotto in baseline (0 violazioni d'ordine Progettazione→Ottenimento→OpereCivili→Test→AsBuilt) e in uno scenario misto con 7 pratiche a stati diversi + tassi custom + dati scavi reali (2 "violazioni" residue, indagate singolarmente: entrambe risultano dati scavi reali impresa con inizio riportato prima del permesso registrato — comportamento voluto e preesistente di `mergeCantieriProgress` da rev.177 "dato reale sempre più autorevole", non un bug della cascata). `node --check` OK. — rev.184: `server.py` + `gantt.html`, richiesta utente: rendere il tasso di scavo (100 m/g, rev.182) configurabile da interfaccia invece che costante nel codice, con possibilità di differenziarlo per Lotto/Impresa/Permesso. Utente ha caricato `server.py` per la prima volta in questa sessione (prima non presente in conversazione) — verificate le convenzioni esistenti (`_require_admin_session`/`_require_staff_session`, pattern `gantt_overrides_col`, `{param:path}` per chiavi con caratteri speciali già usato per `cantiere_key`) e replicate fedelmente.

**Backend**: nuova collezione `gantt_rates_col` (`db["gantt_rates"]`), indice unique su `scope`. Scope è una stringa libera con 4 forme: `"global"`, `"lotto:<ID>"`, `"impresa:<NOME>"`, `"pratica:<CODICE>"` (es. `"pratica:AUT/1/1A"`) — un'unica collezione generica invece di 4 tabelle separate, stesso spirito di `gantt_overrides`. Tre endpoint nuovi, stesso stile dei fratelli `/api/gantt/overrides/*`: `GET /api/gantt/rates` (staff, lista tutte le regole), `PUT /api/gantt/rates/{scope:path}` (admin, upsert `m_giorno`, validato 0-10000), `DELETE /api/gantt/rates/{scope:path}` (admin, rimuove — la pratica ricade sul livello meno specifico successivo). Nessun audit log `_log_admin_action` aggiunto, per coerenza con `gantt_overrides` che non lo usa.

**Frontend — priorità di risoluzione confermata con l'utente**: PERMESSO > IMPRESA > LOTTO > DEFAULT (la più specifica vince). Estratto nuovo campo `_praticaCode` (codice pratica completo, es. "AUT/1/1A" — necessario perché il solo numero già usato in `_pratica` può ripetersi tra serie AUT/NO) su tutte le 155 righe foglia via parsing una tantum della label del gruppo `g2`, stesso meccanismo già usato per `_km` (rev.181); 29 codici pratica unici su 31 righe (2 pratiche fisicamente divise su due zone hanno lo stesso codice — corretto, stessa autorizzazione).

Riorganizzato l'ordine di init, prima sincrono-puro: `applyKmBasedOpereCiviliDurations()` (ora prima passata col solo default `SCAVO_M_GIORNO`, per un render immediato) → nuova `applyGanttRates()` asincrona (fetch `/api/gantt/rates` + `/api/cantieri?lotto=1A` per costruire la mappa comune→impresa, stessa fonte già usata da `mergeCantieriProgress` per l'avanzamento ma fetchata separatamente per non toccare quella funzione già in produzione — leggera ridondanza accettata) → ricalcola durata/fine di Opere Civili col tasso risolto e **aggiorna anche `_baseEnd`**, così la nuova durata diventa la baseline che `propagateGanttDatesFromPermits()` trasla. **Race condition trovata e corretta prima di consegnare**: `applyGanttRates()` non può girare in `Promise.all` insieme a `mergeCantieriProgress()` (dati scavi reali) perché l'ordine di arrivo delle due fetch non sarebbe garantito — un dato scavi reale già arrivato potrebbe essere sovrascritto per corsa dalla stima km-based. Reso sequenziale: `applyGanttRates()` completa per primo, poi `Promise.all([mergeMasterCsv(), mergeCantieriProgress(), fetchGanttOverrides()])` — così i dati scavi reali (`row.real=true`) vincono sempre, garantito dall'ordine e non dal caso.

Limite noto e documentato in codice: lo scope "impresa" è risolvibile solo per gli enti comunali (unico livello a cui i cantieri sono tracciati, stessa convenzione di `mergeCantieriProgress`) — per enti non comunali (AIPO/RFI/SATAP/ecc.) ricade su lotto/default.

**UI admin**: nuovo pulsante "Tassi di produzione" (accanto a "Modalità modifica", solo `IS_ADMIN`) apre un pannello con lista regole esistenti (ordinate per specificità, con autore/data, pulsante rimozione) e form nuova regola (select ambito + campo valore dinamico + m/giorno). Salvataggio/rimozione richiamano `applyGanttRates()` e `buildGantt()` per riflettere subito il cambiamento senza reload.

Testato: risoluzione priorità end-to-end (regola permesso=150 vince su impresa=120/lotto=90/default=80 sulla pratica giusta; pratica senza regole specifiche ricade su lotto=90); verificata l'assenza di race condition (dati scavi reali sovrascrivono correttamente la stima anche eseguendo `mergeCantieriProgress` dopo `applyGanttRates`); pannello UI testato via Chromium/Playwright (lista popolata, toggle campo ambito dinamico corretto). `node --check` OK, `python3 -m py_compile server.py` OK. — rev.183: `gantt.html`, feedback utente su rev.182: la durata di Opere Civili era calcolata in giorni di calendario continui (100% shiftDmy), il cantiere però non lavora sabato/domenica. Nuova `addWorkDaysDmy(d, workDays)`: conta solo lun-ven, se la data di partenza cade di sabato/domenica il conteggio parte dal lunedì successivo (il cantiere non inizia nel weekend). Sostituisce `shiftDmy` nel calcolo della fine di Opere Civili in `applyKmBasedOpereCiviliDurations()` — la durata resta "N giorni lavorativi" (52gg per 5,13km, invariata), ma lo **span di calendario** ora si allunga per includere i weekend attraversati (es. pratica da 52gg lavorativi: prima finiva dopo 52 giorni di calendario continui, ora dopo 72, includendo i 20 giorni di weekend saltati). **Scope volutamente limitato**: la regola tocca solo il calcolo della durata di Opere Civili (l'unica parte del gantt che genera date da una formula, non dal `.mpp`) — non le altre fasi (Progettazione/Test/As Built/Materiali, baseline `.mpp` invariata) né la propagazione da permesso (`propagateGanttDatesFromPermits()`, che trasla per delta di calendario uniforme: preserva comunque la durata lavorativa corretta perché sposta start+end della stessa quantità di giorni, invariante rispetto al weekend). Testato: `addWorkDaysDmy` su partenza infrasettimanale (mercoledì +5gg lav → martedì successivo, corretto) e su partenza di sabato (+1gg lav → slitta a lunedì, corretto); verificato che l'intervallo calendario della pratica più grande contenga esattamente 52 giorni feriali e 20 di weekend. `node --check` OK. — rev.182: `gantt.html`. Due punti dall'utente sull'anteprima rev.181. (1) **Falso allarme chiarito, non un bug**: nello screenshot Opere Civili sembrava iniziare prima del permesso perché nel test avevo chiamato `mergeMasterCsv()` senza poi `propagateGanttDatesFromPermits()` (che in produzione, riga ~1140, viene sempre invocata subito dopo in `Promise.all(...).then()`) — verificato ora con lo shift completo che l'ordine FS regge sempre (Opere Civili parte il giorno dopo la data permesso, mai prima, anche con slittamenti di settimane). (2) **Nuova regola reale**: la durata di Opere Civili nel `.mpp` era uniforme per tutte le 31 pratiche indipendentemente dai km (es. AIPO 0,05km e Cornaredo 5,13km duravano identicamente ~34gg) — sostituita con un calcolo su produzione reale, chiarito con l'utente prima di implementare (100 km/g dichiarato era refuso, confermato 100 **m**/g; pozzetti confermati in parallelo, non sequenziali). Nuova IIFE `applyKmBasedOpereCiviliDurations()`, eseguita una sola volta subito dopo la generazione di `GANTT_ROWS` e **prima** della cattura baseline (riga ~205) — cruciale: la nuova durata diventa essa stessa la baseline che `propagateGanttDatesFromPermits()` trasla quando il permesso slitta, non quella uniforme del `.mpp`. Formula: `giorni = ceil(km*1000 / SCAVO_M_GIORNO)` con `SCAVO_M_GIORNO=100` (costante in cima al file, facilmente ritarabile), minimo 1 giorno. Pozzetti: `POZZETTI_OGNI_M=500` × `POZZETTI_PER_COPPIA=2` — stimati e mostrati nel `sub` (tooltip/popover) ma **non sommati alla durata** (parallelo, come da conferma). Entrambe le costanti/formula scritte in modo generico su `row._km` (rev.181): valide automaticamente anche per gli altri lotti quando verranno aggiunti. Testato in Node: durata esatta su 4 pratiche campione (5,13km→52gg, 0,81km→9gg, 0,05km→1gg, 0,15km→2gg, tutte coerenti con `ceil(km*10)`); propagazione con permesso spostato di +25gg — Opere Civili segue mantenendo 52gg di durata, mai prima del permesso. `node --check` OK. — rev.181: `gantt.html`, feedback utente su rev.180: la % di Progettazione non deve derivare da un conteggio di pratiche ma da un rapporto in metri (km pratiche uscite da IN ATTESA/IN REDAZIONE ÷ km totali), coerente con la convenzione ENRI già in uso altrove (donut/avanzamento scavi km-weighted, NO COMPETENZA mai sommato). **Il flag binario per pratica introdotto in rev.180 resta corretto e invariato** (una singola pratica è o non è ancora in redazione, non ha senso una % intermedia a quel livello) — il cambiamento è tutto nell'**aggregazione**: nuovo campo numerico `_km` su ogni riga foglia di ogni pratica (parsing una tantum del valore già presente nella label del gruppo `g2`, es. "· 5,13 km" → `5.13`; 155 righe valorizzate = 31 pratiche × 5 fasi). Nuovo chip KPI "Progettazione (km): X%" in `buildKpi()` = Σkm(pratiche con Progettazione al 100%) / Σkm(tutte le pratiche) — quest'ultimo somma le km-autorizzazione delle 31 pratiche (21,22 km), non il km fisico dichiarato del Lotto 1A (16,89 km): sono volutamente diversi, perché più enti autorizzano spesso lo stesso tratto fisico (attraversamenti RFI/AIPO/SATAP/ANAS/SERRAVALLE sovrapposti alla tratta del comune), esattamente come già avviene per gli altri aggregati per-pratica della dashboard (Master.csv/`index.html`). `.legend-row` già `flex-wrap:wrap`, il 5° chip non rompe il layout su schermi stretti. Testato in Node: caso con mix di stati/km eterogenei (kmDone=1,01 su kmTot=21,22 → 5%, verificato a mano) e caso senza dati Master.csv (0% netto, nessun NaN, tutte le 31 righe con `_km` valorizzato). `node --check` OK. — rev.180: `gantt.html`, `mergeMasterCsv()` — nuova regola richiesta utente: la fase "Progettazione e presentazione permesso" non ha un proprio stato in Master.csv (che traccia solo l'iter del permesso via `STATO_PERMESSO`), restava quindi sempre alla baseline .mpp (0%) anche a pratica ben avviata. Dedotta ora dallo stesso `STATO_PERMESSO` già letto per la milestone "Ottenimento permesso": `IN ATTESA`/`IN REDAZIONE` → 0% (fase ancora in corso), qualunque stato successivo (`IN FIRMA RDS` in su, incluso `OTTENUTO`) → 100% (uscita dalla fase di redazione = progettazione conclusa). Match esplicito `_pratica`+`_ente` tra la riga Progettazione e la sua milestone (non offset posizionale nell'array) — **volutamente generico**, richiesto in vista dell'estensione ad altri lotti (§TODO rev.178/179): funziona indipendentemente dall'ordine/posizione con cui le righe di un lotto futuro verranno generate, purché portino gli stessi metadati. `NO COMPETENZA` (pct=null da `statoToPct`) esce prima del match: Progettazione resta a baseline, coerente con l'esclusione già in uso altrove nella dashboard. Testato in Node (mock Master.csv, 4 stati + 1 pratica senza dato): `IN REDAZIONE`→0, `IN FIRMA RDS`→100, `PROTOCOLLATO`→100, `OTTENUTO`→100, pratica non presente in Master.csv→0 invariata (baseline). Verificato che `row.real=true` sulla barra non tocca lo stile di rendering (solo `pct` conta per bar, a differenza delle milestone dove `real` influenza lo stroke del diamante) — nessun effetto collaterale visivo. `node --check` OK. — rev.179: `gantt.html`, chiuso il TODO aperto da rev.178 (KPI header non riverificato). Verifica completa: (1) audit codice morto — tutte le 29 funzioni/handler top-level hanno ≥1 chiamata oltre alla definizione, nessuna orfana; `sub`/`_ente`/`_pratica`/`_comune` tutti letti da qualche parte (tooltip, override, merge); nessuna classe CSS residua della vecchia struttura piatta. (2) Editabilità confermata identica al comportamento pre-rev.176: `findGanttRow`/click handler smistano genericamente per `data-group-id` (toggle collasso, sia `g` che `g2`) vs `data-row-id` (popover edit/deps, solo foglie) — testato in Node+jsdom l'intero ciclo apri popover→modifica→PUT `/api/gantt/overrides/1A/{id}`→riga aggiornata in memoria (`manual:true`) su una riga annidata dentro una pratica: id corretto, payload corretto, nessuna regressione. (3) **Bug reale trovato e corretto in `buildKpi()`**: il chip "Permessi ottenuti" contava tutte le righe `t:'ms'` (33, incluse le 2 milestone Materiali Tubi/Pozzetti aggiunte in rev.178) come se fossero permessi — mostrava "0/33" e considerava Tubi/Pozzetti scaduti tra le "Scadenze superate". Bug preesistente in forma equivalente da rev.0 (contava le 3 milestone "Consegna materiali" della vecchia Fase 3), mai notato perché irrilevante finché Materiali era un gruppo minoritario. Fix: nuovo filtro `permits = ms.filter(r => r._pratica)` (i metadati aggiunti in rev.178 permettono di distinguere permesso reale da milestone Materiali senza euristiche su label) — ora "Permessi ottenuti: 0/31" corretto, "Scadenze superate: 0" (Tubi/Pozzetti non più conteggiati). Verificato con screenshot reale via Playwright/Chromium headless (non più solo cairosvg su SVG isolato): bounding-box dei 4 chip KPI non si sovrappongono nemmeno col testo più lungo, SVG gantt contenuto nel wrapper senza overflow. `node --check` OK. — rev.178: **riscrittura strutturale** `gantt.html`, richiesta utente: gerarchia Lotto→Comune→Pratica→Fasi lavorative (rev.176/177 avevano già i dati reali per-ente ma restavano **piatti** per fase, con le 31 pratiche ripetute sotto ognuna delle 4 fasi). Ri-estratti gli stessi dati dal `.mpp` (mpxj, stesso metodo rev.176) ma preservando la gerarchia WBS nativa invece di appiattirla: `GANTT_ROWS` passa da 137 a 192 righe con 3 tipi di header (`g`=comune/zona, livello 1; `g2`=pratica, livello 2 nidificato; foglie `bar`/`ms`=fasi). Ogni pratica ora contiene 5 fasi in sequenza: Progettazione→Ottenimento permesso→Opere Civili (Accantieramento..Pozzetti aggregati, invariato da rev.176)→Test e collaudo→As Built — **Progettazione non era mai stata per-pratica prima** (rev.176 la lasciava esplicitamente "non toccata", dato globale approssimato di Fase1): ora usa le date reali del .mpp (29/04→09/06/2026, identiche per le 31 pratiche, coerente col piano-baseline uniforme già documentato rev.176). "Materiali" (Tubi/Pozzetti) confermato dall'utente come sezione **globale a livello Lotto**, fuori dalla gerarchia comune/pratica — resta un gruppo `g` a sé con le 2 milestone, nessuna dipendenza nel .mpp. Collasso generalizzato a 2 livelli (`totalH`/render-loop/`_groupRowCount` in `buildGantt()`, righe ~424-650): un `g` collassato nasconde anche le `g2` figlie; una `g2` collassata nasconde solo le proprie righe foglia, l'header pratica resta visibile col conteggio. **Fix conseguente** (rimuove fragilità nata in rev.176): `parsePraticaFromLabel()`/`parseComuneFromGanttLabel()` (regex sulla label, già una pezza rev.176) eliminate — ogni riga foglia porta ora `_ente`/`_pratica`/`_comune` come metadati diretti assegnati in generazione, `mergeMasterCsv()`/`mergeCantieriProgress()` matchano su quelli, non più su pattern testuali. `propagateGanttDatesFromPermits()` (rev.177) **non toccata**: già generica via `GANTT_DEPS`/BFS, funziona identica sulla nuova struttura (verificato). `GANTT_DEPS` rigenerato 1:1 dal .mpp per la nuova indicizzazione (124 dipendenze, stessa catena FS lag-0 di rev.176, solo ID righe cambiati). Testato: `node --check` OK; script isolato in Node (mock localStorage/fetch) — 0 dipendenze rotte, conteggi `_groupRowCount` corretti (pratica=5, comune Cornaredo=30, materiali=2, comune Pero=78), `mergeMasterCsv`/`mergeCantieriProgress` verificati con CSV/cantieri mock (match corretto via metadati, enti non comunali invariati); render completo via jsdom — altezza SVG corretta espansa (5476) e dopo collasso comune (3266, -78 righe) e collasso singola pratica (5326, -5 righe); controllo visivo screenshot (cairosvg) OK, indentazione a 3 livelli leggibile. TODO aperto: estendere ad altri 3 lotti se richiesto (dati già nel .mpp, WBS 1.1.2/1.1.3/1.1.4 non ancora trasformati); KPI header (`buildKpi`) non ricalibrato per il nuovo numero di bar (124 invece di ~90) — valori aggregati restano corretti ma non riverificato visivamente il layout dei box KPI con lo screenshot.
_Ultimo aggiornamento: 2026-07-10 (rev. 177) — rev.177: `gantt.html`, feedback utente su rev.176 (2 punti). (1) Conferma: le dipendenze **sono ancora renderizzate** (connettori SVG, hover-highlight, popover) — rev.176 ha sostituito solo il contenuto di `GANTT_ROWS`/`GANTT_DEPS`, non la logica di disegno (`ganttSuccessorsOf`, loop righe ~786+ invariati). (2) **Gap reale**: le date di Fase 5/6/7 non si allineavano alle previsioni impresa — `mergeMasterCsv()` aggiorna solo la data della milestone Fase 2 (pratica), le fasi a valle restavano fisse alla baseline .mpp anche se la pratica slittava (limite noto da rev.166, mai chiuso: "le dipendenze restano solo informative/visive, non ricalcolano le date dei successori"). Nuova `propagateGanttDatesFromPermits()`: per ogni milestone con dato reale (`row.real` da `mergeMasterCsv`), calcola lo scostamento in giorni tra `row.date` (reale/previsto) e `row._baseDate` (baseline .mpp, salvata a parte subito dopo la definizione di `GANTT_ROWS`) e lo propaga in cascata ai successori via `GANTT_DEPS`/`ganttSuccessorsOf` (BFS con `visited` anti-loop), traslando `start`/`end` di ciascuna fase preservandone la durata (tutte le dipendenze sono FS lag 0/10/30/70, quindi uno spostamento uniforme del predecessore trasla l'intera catena senza ricalcolo durate). Non sovrascrive righe già marcate `row.real` da `mergeCantieriProgress()` (avanzamento scavi reale, più autorevole di uno spostamento inferito dal solo permesso) né gli override manuali admin (`applyGanttOverrides()` chiamata dopo, vince sempre). Wired in `Promise.all(...).then()` tra i merge e `applyGanttOverrides()`. Nuovo flag `row.shiftedFromPermit` per distinguere in futuro (UI non ancora aggiornata) una riga traslata per inferenza da una con dato reale diretto. Testato isolando lo script (Node, mock scostamento +5gg su una pratica): propagazione corretta su Fase5→6→7, altri enti invariati, righe `real=true` protette da sovrascrittura. `node --check` OK.
_Ultimo aggiornamento: 2026-07-10 (rev. 176) — rev.176: **riscrittura dati** `gantt.html`, richiesta utente: allineare il Gantt a come effettivamente pianificato nel Project reale (`Gantt_Cluster_1_e_2.mpp`, caricato dall'utente, letto via `mpxj`/`UniversalProjectReader` — Python, `org.mpxj.reader` non `net.sf.mpxj`, versione mpxj 16.5 col package rinominato) invece che dai dati approssimati/da screenshot usati finora (rev.0/rev.154). Scoping via 2 domande all'utente: (1) ambito confermato **solo Lotto 1A** (il .mpp copre tutti e 4 i lotti/927 task, ma la pagina resta mono-lotto); (2) Fase 5/6/7 (Opere Civili/Test-Collaudo/As-Built) passano da **aggregate per comune** (3 barre, dato di fantasia) a **per ente/permesso**, fedeli al .mpp — 31 pratiche reali nel Lotto 1A (non 18 come recitava il vecchio titolo Fase 2, mai verificato contro un .mpp reale prima d'ora). Sostituiti integralmente `GANTT_ROWS` (137 righe) e `GANTT_DEPS` (127 dipendenze, tutte **esatte 1:1** col .mpp — non più `approx:true`, perché ora ogni riga corrisponde a una singola sotto-attività reale, non più a un'aggregazione): catena FS lag-0 Progettazione→Ottenimento permesso→Opere Civili→Test/Collaudo→As-Built, identica per tutti i 31 enti (il .mpp modella un piano-baseline uniforme: stesse date per ogni pratica — 21/07 ottenimento, 22/07-25/08 opere civili, 26/08-28/08 collaudo, 31/08-02/09 as-built — non scaglionato come i vecchi dati). Fase 1/3/4 non toccate nei contenuti (il .mpp non modella Approvvigionamento/Cantierizzazione con date reali a livello Lotto 1A, restano i valori pre-esistenti) tranne Fase 1 il cui range è stato aggiornato al dato reale (29/04→09/06/2026). Fix conseguente necessario: `parseComuneFromGanttLabel()` (usata da `mergeCantieriProgress()` per il merge avanzamento scavi) cercava `Comune di X` **a fine label** — pattern morto con le nuove label per-ente tipo "9 · COMUNE DI PERO - AUT/13/1A" (codice pratica in coda, non il comune); regex estesa a matchare "COMUNE DI X" ovunque nella stringa. Enti non-comunali (AIPO/RFI/SATAP/SERRAVALLE/ANAS/PROVINCIA/EST VILLORESI) non hanno mai avuto un cantiere associabile per nome comune: restano a baseline finché `/api/cantieri` non traccia anche il predecessore per pratica, non solo per comune (limite pre-esistente, non introdotto qui). `node --check` OK su script estratto, verifica programmatica indici `GANTT_ROWS`/`GANTT_DEPS` (nessun riferimento pendente). TODO aperto: estendere a tutti e 4 i lotti se richiesto in futuro (dati già estratti nel .mpp, non ancora trasformati in righe).
_Ultimo aggiornamento: 2026-07-10 (rev. 175) — rev.175: **fix bug** `imprese.html`, tab "Aggiorna pratiche" — segnalato dall'utente: un'impresa spuntava il checkbox Convenzione senza cambiare stato e il flag non arrivava mai in coda approvazioni admin. Causa: due bug distinti nello stesso blocco `addToQueueBtn`, stesso pattern strutturale di rev.174 (regressione probabile della stessa rev.120 che ha introdotto le checkbox). (1) Guard pre-invio riga 920 (`if (!stato && !noteVal && !prevRilascio) return ...`) non considerava Polizza/Convenzione: un'impresa che toccava *solo* quei checkbox veniva bloccata silenziosamente nel click, nessun campo in coda. (2) Anche superato il guard, `skipChecks = !stato || stato==='NO COMPETENZA' || stato==='OTTENUTO'` escludeva Polizza/Convenzione dal payload ogni volta che `!stato` (nessun cambio stato) — footgun identico al `!stato` di rev.174 su `DATA_PREVISTA_RILASCIO`, stesso file, stessa root cause mai generalizzata dopo il primo fix. Fix: (a) nuova variabile `checksVisible` (basata su `wrap-convenzione` non `display:none`, cioè non in stato NO COMPETENZA) sostituisce `skipChecks` per Polizza/Convenzione nel payload — invio ora indipendente da `stato`; `skipChecks` resta invariato solo per Nulla Osta Necessario, che ha senso solo insieme ad AUTORIZZAZIONE; (b) guard pre-invio esteso con `polizzaChanged`/`convenzioneChanged` (confronto checkbox corrente vs valore originale in `SELECTED`), permette submit anche a stato/nota/data invariati se uno dei due flag è stato effettivamente toccato. `mappa_impresa_caricamento.html` VERIFICATO in sessione successiva (stesso giorno): il payload (`skipChecks`) qui era già corretto — nessun `!stato`, coerente con rev.122 — quindi solo il bug (a) era assente; presente invece il bug (b) identico: guard pre-invio (`prAddToQueue`, riga ~3956) non contava Polizza/Convenzione. Fix applicato: stesso pattern, `polizzaChanged`/`convenzioneChanged` confrontati contro `PR_SELECTED`. `node --check` OK (script estratto e verificato isolatamente). TODO chiuso.
_Ultimo aggiornamento: 2026-07-10 (rev. 174) — rev.174: **fix bug** `imprese.html`, tab "Aggiorna pratiche" — segnalato dall'utente: la data prevista rilascio inserita dall'impresa non arrivava mai a destinazione (le altre info sì), mentre la stessa funzionalità su `mappa_impresa_caricamento.html` risultava già corretta. Confronto diretto tra i due file: `addToQueueBtn` (`imprese.html`, riga ~945) azzerava `DATA_PREVISTA_RILASCIO` con la condizione `(!stato || stato==='OTTENUTO' || stato==='NO COMPETENZA') ? '' : ...`, mentre l'omologo `prAddToQueue` in `mappa_impresa_caricamento.html` (corretto in sessione precedente non documentata su questo file, v. nota sotto) usa `(stato==='OTTENUTO' || stato==='NO COMPETENZA') ? '' : ...` — senza `!stato`. Il termine extra faceva sì che ogni aggiornamento **senza cambio di stato** (sola nota, o sola data previsione) perdesse silenziosamente la data prevista, anche se l'impresa l'aveva compilata. Fix: rimossa la condizione `!stato` dall'azzeramento in `imprese.html`, allineandola a `mappa_impresa_caricamento.html`. Effetto collaterale scoperto e corretto nello stesso punto: il controllo minimo pre-invio (riga 919, `if (!stato && !noteVal) return ...`) non considerava affatto la data prevista — un'impresa che compilava *solo* quella (nessuno stato, nessuna nota) veniva bloccata con un messaggio d'errore fuorviante ("Seleziona un nuovo stato oppure inserisci una nota") pur avendo fornito un dato valido; ora il controllo include anche `!prevRilascio`. Verificato lato backend (`server.py`): `DATA_PREVISTA_RILASCIO` non ha alcun filtro speciale in `_apply_changes_to_df` (trattato come campo generico di `fields`) — bug era 100% frontend, isolato a questo file. `node --check` OK.
_Ultimo aggiornamento: 2026-07-10 (rev. 173) — rev.173: **server.py + admin.html** — richiesta utente: poter correggere i dati di una submission impresa (`pending_updates`) **prima** di approvarla, non solo approvare/rifiutare as-is (stesso principio di rev.171/172 sui cantieri). Nuovo `PUT /api/admin/pending-updates/{sub_id}`: sostituisce l'array `changes` della submission, bloccato con 409 se `status != 'pending'` (non modificabile dopo approvazione/rifiuto); traccia `edited_at`/`edited_by`, audit su `admin_actions_col`. `admin.html`: bottone "✎ Modifica" tra Approva/Rifiuta su ogni card pending → modal con textarea JSON precompilata (`JSON.stringify(sub.changes)`), validazione JSON lato client prima del salvataggio. Scelto un editor JSON grezzo invece di un form per-campo perché `changes` ha due schemi diversi (`type:'update'` → `{tratta_id,ente,tipo_permesso,fields:{...}}`; `type:'new'` → record CSV flat) — un form dinamico per entrambi avrebbe richiesto molta più superficie di codice per lo stesso risultato, e l'utente (dev) è a suo agio col dato grezzo. `py_compile`/`node --check` OK.
_Ultimo aggiornamento: 2026-07-10 (rev. 172) — rev.172: **server.py + admin.html** — nuova tab "Cantieri" in admin, su richiesta utente ("si può aggiungere una sezione per modificare questi dati da admin?", nato da necessità di rimuovere avanzamenti di test caricati per errore da Sielte via `imprese_scavi.html`). Prima non esisteva alcun modo per correggere/azzerare **un singolo** cantiere: l'unico bottone esistente (`DELETE /api/admin/cantieri/reset`, tab File correnti) svuota **tutta** la collection e risincronizza da Master.csv. 2 nuovi endpoint: `PUT /api/admin/cantieri/{cantiere_key}` (correzione diretta — stato, metri **assoluti** non in accumulo a differenza di `POST /api/imprese/cantieri/{key}` che usa sempre `$inc`, tecnica, date, note; opzionale `clear_log` per svuotare lo storico) e `DELETE /api/admin/cantieri/{cantiere_key}/reset` (riporta un solo cantiere a pristino — non_avviato/0 metri/log vuoto — senza toccare gli altri né ricrearlo, metadati invariati). `admin.html`: tabella cantieri con filtri lotto/stato/ricerca, bottoni "Modifica" (form inline) e "Reset" (con conferma) per riga. `py_compile`/`node --check` OK, th/td tabella verificati (9/9).
_Ultimo aggiornamento: 2026-07-10 (rev. 171) — rev.171: **fix bug** `imprese.html`, tab Solleciti — segnalato dall'utente con CSV reale (pratica AUT/23/1A, Comune di Rho): la lista "Nuovo sollecito — seleziona la pratica" mostrava 4 tratte/stato PROTOCOLLATO per una pratica che nella realtà ha **2** tratte, entrambe già passate a NECESSARIA INTEGRAZIONE (verificato via pannello "tratte collegate" di `imprese.html`, calcolo indipendente senza filtri di stato → confermava 2). Causa: `renderSolPratiche()` itera su `PRATICHE` grezzo, che contiene **tutta la storia** delle righe (Master.csv è append-only, il backend accoda righe invece di sovrascrivere — v. §8 convenzioni) — per TR_0223/TR_0228 esistono 5 righe storiche ciascuna (IN REDAZIONE→IN FIRMA RDS→INVIATO→PROTOCOLLATO→NECESSARIA INTEGRAZIONE); il filtro di esclusione stati (`SOL_STATI_ESCLUSI`) veniva applicato riga per riga **senza prima deduplicare allo stato più recente per tratta** (a differenza di `renderPratiche()`/tab "Aggiorna pratiche", che dedup correttamente via `latestMap` da rev.141). Le righe storiche superate INVIATO/PROTOCOLLATO di entrambe le tratte sopravvivevano al filtro (non essendo nel set escluso) ed entravano nel conteggio: 2 tratte × 2 righe storiche = 4. Fix: aggiunto lo stesso step di dedup (`TRATTA_ID+ENTE+TIPO_PERMESSO`, tiene la riga con `DATA_ULTIMA_MODIFICA` più recente) **prima** di filtrare/raggruppare in `renderSolPratiche()`. Con questo fix AUT/23/1A esce correttamente dalla lista (nessuna tratta ancora sollecitabile). Nello stesso filone era stato corretto anche un problema distinto ma collegato (chiave di raggruppamento senza lotto, poteva confondere pratiche con lo stesso numero su lotti diversi — usa ora `_codice` invece del solo numero pratica) e, su tabella diversa, la priorità stato-live-vs-snapshot in `renderSolleciti()` (le tratte già registrate mostravano lo stato congelato al momento del sollecito invece di quello attuale della pratica). `node --check` OK.
_Ultimo aggiornamento: 2026-07-10 (rev. 170) — rev.170: **fix bug preesistente + audit generale** `imprese.html`, richiesta utente ("controlla se ci sono errori... in generale funzioni morte o codice che dà errori"). Trovato e corretto 1 disallineamento reale, preesistente (non introdotto in questa sessione): riga di loading della tabella Solleciti (`solBody`, 14 colonne) usava `colspan="13"` — lo spinner di caricamento non si estendeva su tutta la larghezza. Corretto a `colspan="14"`. Audit sistematico (regex + `node --check`) su: funzioni dichiarate mai chiamate (nessuna, 34/34 referenziate), funzioni/variabili duplicate nello stesso scope (nessuna), ID DOM duplicati (1 falso positivo: `_warnCancel`, due branch if/else mutuamente esclusivi dello stesso overlay dinamico, mai coesistenti), riferimenti a `$('id')` senza corrispondente id statico (1 falso positivo: `new-ente-custom`, creato dinamicamente e già guardato da `? :` ovunque usato), chiamate a funzioni mai definite nel file (tutti falsi positivi — parole italiane in stringhe/commenti seguite per caso da `(`, limite del regex, non del codice).
_Ultimo aggiornamento: 2026-07-10 (rev. 169) — rev.169: `imprese.html`, tabella "Aggiorna pratiche" — 2 richieste utente sulla colonna "Data previsione" (`DATA_PREVISTA_RILASCIO`) aggiunta in rev.168: (1) riposizionata dopo "Update" (era tra "Data agg." e "Update"); (2) tutte le 10 colonne rese ordinabili stile Excel — click header ordina, click di nuovo inverte asc/desc, comparator type-aware (`PRAT_COL_TYPES`: date via `parseDate()` esistente, numero per Lunghezza, testo con `localeCompare(...,'it',{numeric:true})` per le altre) via nuovo stato `PRAT_SORT{key,dir}`. Su richiesta successiva ("non si capisce che si può ordinare"): icona `⇕` sempre visibile su ogni header ordinabile (prima appariva solo su quello attivo), diventa `▲`/`▼` colorata (classe `.active`) sulla colonna correntemente ordinata.
_Ultimo aggiornamento: 2026-07-10 (rev. 168) — rev.168: `imprese.html`, tabella "Aggiorna pratiche" (`renderPratiche()`/`pratBody`) — richiesta utente: aggiunta colonna "Data previsione" (`DATA_PREVISTA_RILASCIO`, già presente nel modello dati/form ma non in questa tabella). Colonne 9→10, colspan aggiornati sui 3 placeholder (loading/errore/vuoto), `.main` allargato 1400px→1600px per far spazio. Continua in rev.169 (riposizionamento + sort).
_Ultimo aggiornamento: 2026-07-07 (rev. 144) — rev.144: `mappa_impresa_caricamento.html` — richiesta utente: click su pratica in tab "Pratiche" (lista `prList`) evidenzia in mappa tutte le tratte collegate, non solo quella selezionata. Nuovo sistema multi-layer `_prHighlightLayers`/`highlightPrTratte(tratteIds)`/`clearPrHighlight()` (separato da `_highlightedLayer` esistente, usato solo dalla ricerca testuale — nessuna interferenza). `prSelectRow()` riusa `PR_SIBLINGS` (già calcolato per stesso PRATICA+ENTE+TIPO_PERMESSO) per costruire il set di `TRATTA_ID` da evidenziare, poi `map.fitBounds()` sul gruppo. Clear richiamato su "Annulla" (`prCancelEdit`) e dopo `_prFinalizeAdd` (form chiuso). Nota: nessuna colonna "Data ottenimento"/tabella Solleciti in questo file (verificato via grep) — il campo omonimo a riga ~961 è l'input impresa per stato OTTENUTO, feature diversa, non toccata da rev.142/143.
_Ultimo aggiornamento: 2026-07-09 (rev. 167) — rev.167: `hub.html`, richiesta utente: label campo login da "Nome e cognome" a "Nome e Cognome o Nome Impresa" (riga ~817, coerente col fatto che le imprese accedono con ragione sociale, non nome persona); rimosso placeholder di esempio "Es. Mario Rossi" (riga ~819, ora vuoto salvo prefill `savedUser` per utente già memorizzato). Nessun impatto su logica JS.\n_Ultimo aggiornamento: 2026-07-08 (rev. 166) — rev.166: `gantt.html`, richiesta utente: predisporre il Gantt ad aggiornarsi in automatico con (1) le previsioni inserite dalle imprese sulle pratiche in `index.html`/`imprese.html` e (2) l'avanzamento scavi delle imprese di scavi. Punto (1) **era già implementato** (rev.166 di questa stessa sessione la conferma, nessun codice nuovo): le milestone Fase 2 leggono già `DATA_PREVISTA_RILASCIO` da Master.csv (`mergeMasterCsv()`) come data quando la pratica non è ancora OTTENUTA. Punto (2) **nuovo**: aggiunta `mergeCantieriProgress()`, fetch `GET /api/cantieri?lotto=1A` (stesso endpoint di `scavi.html`, staff-only), raggruppa i cantieri per `comune` e aggiorna le 3 barre "Opere Civili" (Fase 5, righe id 40/41/42) con: `pct` = `metri_scavati/metri_totali` sommati sul comune (stessa convenzione di `scavi.html`), `start`/`end` = min/max tra `data_inizio_effettiva`/`data_fine_effettiva` (fallback su `_prevista`), conversione data ISO (`input type=date`) → gg/mm/aaaa (`isoToDmy()`). Match comune via regex su label riga (`Comune di (.+)$`) case-insensitive. Se `/api/cantieri` non restituisce ancora cantieri per il lotto (caso attuale), esce senza toccare la baseline. Wired nel `Promise.all` di caricamento insieme a `mergeMasterCsv()`/`fetchGanttOverrides()`; un override manuale admin vince sempre anche su questo merge (applicato dopo in `applyGanttOverrides()`). **Non incluso** (fuori scope, da confermare): propagazione automatica delle date verso Fase 6/7 tramite `GANTT_DEPS` — le dipendenze restano solo informative/visive, non ricalcolano le date dei successori. `node --check` OK.
_Ultimo aggiornamento: 2026-07-08 (rev. 165) — rev.165: `gantt.html`, feedback utente su rev.164 (screenshot: connettori dipendenza illeggibili, sovrapposti/incrociati quando più task condividono lo stesso predecessore, es. le 3 "Consegna materiali" da "Richiesta forniture"). Fix: (1) connettori da gomito retto (`L`) a curva bezier morbida (`C`, control point a metà distanza) — elimina i segmenti dritti sovrapposti anche con punto di partenza condiviso; (2) hover-highlight: nuova classe `.gantt-dep` + `data-dep-pred`/`data-dep-succ` su ogni path/freccia, listener `mousemove` sul task (`data-row-id`) chiama `_applyGanttDepHighlight()` che evidenzia (`.hi`, opacità 100%+stroke 2.2px) solo le dipendenze collegate al task sotto il cursore e attenua tutte le altre (`.dim`, opacità 8%); reset su `mouseleave`. Nessuna modifica a `GANTT_DEPS`/logica dati, solo rendering SVG e interazione. `node --check` OK.
_Ultimo aggiornamento: 2026-07-08 (rev. 164) — rev.164: `gantt.html` + `backend/server.py`, richiesta utente: dipendenze tra task ora **editabili**, non più solo in sola lettura. In "Modalità modifica" (admin/admin2) il popover di editing di ogni riga mostra ora `ganttDepsEditHtml()` (select Predecessore + select Tipo FS/SS + input Ritardo giorni) al posto della vecchia vista testuale `ganttDepsHtml()` (quest'ultima resta usata solo nel popover read-only fuori da edit mode, invariato). Salvataggio: `saveGanttEdit()` include `dep_pred`/`dep_type`/`dep_lag` nel payload esistente (stesso bottone Salva, stesso endpoint `PUT /api/gantt/overrides/{lotto}/{row_id}`), validazione client self-reference (pred≠rowId). `applyGanttOverrides()` applica l'override alla mappa runtime `GANTT_DEPS` (aggiunge/sostituisce se `dep_pred` valorizzato, cancella la entry se `dep_pred` vuoto/null = "rimuovi dipendenza"). Backend: whitelist campi PUT estesa con `dep_pred`/`dep_type`/`dep_lag` (validati: pred intero ≠ row_id, type in FS/SS, lag intero ≥0), GET restituisce anche questi 3 campi. Nessuna modifica a `GANTT_DEPS` baseline (dati .mpp rev.154) né a `gantt_overrides` collection/schema Mongo. Non implementato: validazione cicli multi-hop (solo self-reference bloccata). `py_compile`/`node --check` OK.
⚠️ **Nota collisione numerazione**: questa sessione (lavoro su `gantt.html`, iniziato da zip caricato) non vedeva le voci rev.155-163 (session parallela su `index.html`/`polizze_convenzioni.html`) a causa di troncamento nella lettura iniziale del brief (file da 649 righe, il tool di lettura ha troncato il blocco centrale). Le 3 modifiche gantt di questa sessione erano state inizialmente numerate 155/157 (collisione), corrette qui a 164/165/166 — contenuto invariato, solo rinumerate. Nessun impatto sul codice, solo sul changelog.
_Ultimo aggiornamento: 2026-07-08 (rev. 163) — rev.163: **fix bug** `index.html`, popup "Storico Stato" (righe ~4126-4149) — segnalato dall'utente da screenshot (AUT/11/2A): il fix multi-tratta di rev.161 non funzionava, storico ancora duplicato (6 righe con data odierna 07/07 + 6 righe con date reali 22/04-30/04, metà con nota vuota). Causa: `isNoteOnlyContinuation` (rev.159) confrontava solo `stato+date`, senza sapere se la riga proviene dalla stessa tratta fisica o da una tratta diversa — sulla seconda tratta di una pratica multi-tratta (stesso stato+dum della prima, nota diversa) scattava per errore come se fosse un vero aggiornamento successivo sulla stessa tratta, spostando la data a `DATA_UPDATE` (qui uguale per tutte le righe del CSV, 07/07, timestamp di batch non di evento reale) invece di lasciarla al `mergeTarget` di rev.161 (che infatti non trovava mai corrispondenza, perché la data era già stata spostata). Verificato sui dati reali Master.csv: AUT/11/2A ha 2 tratte fisiche (righe 657-662 e 850-855) con stessi 6 stati/date ma note diverse (vuoto vs "Consorzio Naviglio Olona"). Fix: ogni entry in `praticaStatoRaw` ora traccia anche il set di `TRATTA_ID` (`trattaIds`) che vi hanno contribuito; la continuazione solo-nota (rev.159, sposta la data a `DATA_UPDATE`) scatta solo se la STESSA tratta ripropone lo stesso stato+dum con nota diversa; se la tratta è diversa (caso multi-tratta) si accorpa nell'entry esistente senza toccare la data, tenendo la nota più informativa (comportamento rev.161 originariamente inteso). Verificato via simulazione Node sui 12 valori reali delle 2 tratte di AUT/11/2A → 6 righe corrette con date reali, nessun duplicato. `node --check` OK su tutti i blocchi script.
_Ultimo aggiornamento: 2026-07-08 (rev. 162) — rev.162: **fix logico** `polizze_convenzioni.html` (riga ~469-476) — segnalato dall'utente: approvando "convenzione necessaria" per una pratica (via impresa), questa compariva anche nella tabella Polizze con stato `NO`, anche se il checkbox polizza non era flaggato. Causa: conflitto di vocabolario sulle stesse colonne `CONVENZIONE`/`POLIZZA` del Master.csv — `imprese.html`/`mappa_impresa_caricamento.html` (rev.120/122) scrivono booleano `SI`/`NO` (checkbox "è necessaria?", **sempre entrambi i campi**, anche quello non flaggato), mentre `polizze_convenzioni.html`/backend (`_POL_CONV_ALLOWED_VALUES`) si aspettano uno stato di workflow (`NECESSARIA`→`RICHIESTA RDS`→`INVIATA`→`EMESSA`); il filtro di inclusione riga (`if (!conv && !pol) continue`) considerava "NO" un valore valido (non vuoto) e la pratica finiva comunque in tabella. Su richiesta esplicita utente (scartata l'opzione di separare le colonne o cambiare `imprese.html`): normalizzazione client-side in `polizze_convenzioni.html`, PRIMA del filtro — `SI`→`NECESSARIA` (entra in pagina, allineato al workflow admin), `NO`→stringa vuota (esclusa, come se il campo non fosse mai stato valorizzato). I valori di workflow già impostati dall'admin passano invariati (non sono `SI`/`NO`). Il valore scritto nel Master.csv resta quello originale (`SI`/`NO`) finché un admin non lo modifica esplicitamente da questa pagina (che allora scrive uno stato di workflow reale, sovrascrivendo il booleano). Verificato sui dati reali: AUT/4/2B e NO/3/2B (conv=SI, pol=NO) ora compaiono solo in Convenzioni (come NECESSARIA), non più in Polizze. `node --check` OK su tutti i blocchi script.
_Ultimo aggiornamento: 2026-07-08 (rev. 161) — rev.161: **fix bug** `index.html`, popup "Storico Stato" (riga ~4127-4144) — segnalato dall'utente da screenshot (AUT/20/1A, provato anche da modal Cluster dopo rev.160): stessa data/stato ripetuti 6-7 volte con note diverse. Causa: la pratica copre 14 tratte fisiche distinte (attraversamenti multipli sotto la stessa autorizzazione/PRATICA+lotto); ogni tratta ha una riga separata nel Master.csv con lo stesso cambio stato reale (stessa `dum`) ma nota diversa (descrizione del singolo attraversamento, es. "SS 33 km 12+330 Rho", "Sottopasso A50 e E32..."). Il dedup precedente (rev.156-159) confrontava anche la nota (`s.note === note`), quindi ogni tratta produceva una riga di storico distinta invece di essere riconosciuta come lo stesso evento. Fix: la logica di continuazione solo-nota (rev.159, invariata) resta per distinguere un vero aggiornamento successivo (via `DATA_UPDATE` divergente); quando invece stato+data coincidono e non c'è divergenza di `DATA_UPDATE` (caso multi-tratta), la riga viene ora accorpata nell'entry già esistente invece di essere duplicata, tenendo la nota più informativa (quella più lunga, es. "Prot. 22614 - SS 33 km 12+330..." invece di "Prot. 22614" nudo). Verificato via simulazione Node su dati reali AUT/20/1A: 7 righe IN REDAZIONE/IN FIRMA RDS duplicate → 1 riga con nota più ricca. **Nota collaterale non toccata**: il popup "Note" (`praticaNotesRaw`) mostra ancora tutte le note distinte per tratta (comportamento voluto lì: sono descrizioni diverse di attraversamenti diversi, informazione utile da non accorpare). `node --check` OK su tutti i blocchi script.
_Ultimo aggiornamento: 2026-07-08 (rev. 160) — rev.160: **feature/coerenza** `index.html`, richiesta utente: rendere cliccabile lo stato anche nei modal aperti dalle barre "Avanzamento per Lotto" (`_colLottoRefresh`, riga ~5226) e "Avanzamento per Cluster" (`_colClRefresh`, riga ~5295), stesso popup "Storico Stato" già presente nella tabella "Tutte le pratiche" — nessuna logica nuova, riuso del listener globale già esistente (`document.addEventListener('click', ...)` su `td.tbl-stato-click[data-cod]`, invariato) e delle regole di data/ordinamento già corrette in rev.156-159 (si applicano automaticamente, stessa fonte dati `p.statoHistory`/`praticaStatoRaw`). Modifica in entrambe le funzioni: (1) aggiunta `window._praticaStatoMap[p.codice] = p.statoHistory || []` accanto alla riga già esistente per `_praticaNoteMap`; (2) cella STATO da `<td>` semplice a `<td class="tbl-stato-click" data-cod="${p.codice}" ... cursor:pointer>`. **Gap identico non toccato** (segnalato, non richiesto): i modal KPI (`_kpiApplyFilter`, riga ~4458) e "Prev. Rilascio" (`_prevApplyFilter`, riga ~4612) hanno celle Codice e Stato entrambe non cliccabili — né note né storico stato disponibili lì. `node --check` OK su tutti i blocchi script.
_Ultimo aggiornamento: 2026-07-08 (rev. 159) — rev.159: **fix logico** `index.html`, popup "Storico Stato"/"Note" (riga ~4122-4136) — su segnalazione utente, correzione della data mostrata per gli update **solo-nota**: finora una riga di aggiornamento senza cambio stato (dum invariato per design, v. rev.146/§backend) ereditava come data la stessa `dum` del cambio stato precedente, facendo sembrare la nota inserita quel giorno invece che il giorno reale dell'update. Fix: rilevata la continuazione solo-nota confrontando ogni riga con le entry già accumulate per la stessa pratica — se esiste già un'entry con **stesso stato e stessa dum**, questa riga è per costruzione un update solo-nota (un vero cambio stato avrebbe una `dum` diversa), e la sua data mostrata diventa `DATA_UPDATE` (vero timestamp dell'inserimento) invece di `dum`. Verificato sul caso reale NO/29/1A: riga PROTOCOLLATO con nota "Torrente Lura-Rho" (dum=02/07, evento reale) e riga PROTOCOLLATO con nota "protocollo V1.2026.0075228..." (dum=02/07 invariato, dataUpdate=08/07) → ora quest'ultima mostra correttamente 08/07/2026. Effetto collaterale positivo: con date ora effettivamente diverse tra le due righe, l'ordinamento si sistema naturalmente senza dover ricorrere al tie-break `upd` di rev.158 (lasciato comunque attivo come rete di sicurezza per eventuali casi limite con `DATA_UPDATE` anch'essa coincidente). `node --check` OK su tutti i blocchi script.
_Ultimo aggiornamento: 2026-07-08 (rev. 158) — rev.158: **fix bug** `index.html`, popup "Storico Stato" — segnalato dall'utente da screenshot (NO/29/1A): due righe PROTOCOLLATO con la stessa data 02/07/2026 (una il cambio stato originale, una un update **solo-nota** dell'impresa successivo, che non cambia `DATA_ULTIMA_MODIFICA`/`dum` per design — v. rev.146) apparivano nell'ordine sbagliato (nota vecchia sopra quella nuova). Causa: a parità di data e di stato, il tie-break rev.157 (indice pipeline `STATI`) non discrimina — il sort ricadeva sull'ordine grezzo del CSV. Fix: aggiunto un terzo livello di tie-break su `DATA_UPDATE` (campo `upd`, ora salvato per ogni entry in `praticaStatoRaw`, riga ~4136) — a parità di data e stato, l'update più recente (anche se solo-nota) sale in cima. La colonna DATA mostrata resta invariata (`dum`, coerente con richiesta utente in rev.156): questo tie-break tocca solo l'**ordinamento**, non il valore visualizzato. `node --check` OK su tutti i blocchi script.
_Ultimo aggiornamento: 2026-07-08 (rev. 157) — rev.157: **fix bug** `index.html`, popup "Storico Stato" (riga ~6138) — segnalato dall'utente da screenshot (AUT/14/2B, INVIATO e PROTOCOLLATO stesso giorno 16/06/2026): mancava ancora il tie-break sull'ordine di iter già documentato come fatto in rev.155 — verificato che quella modifica **non era presente** nel file caricato in questa sessione (probabile disallineamento tra brief e codice reale). Aggiunto ora: a parità di data, sort secondario su `STATI.indexOf(stato)` decrescente (pipeline già esistente riga 2133, `...INVIO PRELIMINARE, INVIATO, PROTOCOLLATO...`) — lo stato più avanzato nell'iter appare più in alto (più recente). `STATI` è `const` di top-level in uno `<script>` precedente (riga 2133), non su `window`; referenziato con guard `typeof STATI !== 'undefined'` per sicurezza, ma essendo scope lessicale di documento condiviso tra `<script>` classici (non moduli) risulta comunque visibile qui. `node --check` OK su tutti i blocchi script.

_Ultimo aggiornamento: 2026-07-08 (rev. 156) — rev.156: **fix bug** `index.html`, popup "Storico Stato"/"Note" (riga ~4126) — segnalato dall'utente da screenshot: pratica AUT/14/2B mostrava la stessa data (08/07/2026) su tutte e 4 le righe di storico invece delle date reali dei cambi stato (10/06, 11/06, 16/06, 16/06). Causa: `noteDateRaw = dataUpdate || dum || ''` dava priorità a `DATA_UPDATE` (timestamp di refresh globale del CSV, uguale su tutte le righe toccate nello stesso upload) su `dum`/`DATA_ULTIMA_MODIFICA` (data specifica del singolo cambio stato). Il bug restava nascosto sulla maggior parte delle pratiche perché `DATA_UPDATE` è tipicamente vuoto sulle righe storiche più vecchie (valorizzato solo sull'ultimo upload) — si manifesta quando un'intera pratica viene caricata/toccata in un unico batch (tutte le righe con `DATA_UPDATE` uguale). Fix: invertita la priorità, `noteDateRaw = dum || dataUpdate || ''`. Corregge sia il popup Storico Stato che il popup Note (stessa riga alimenta `praticaStatoRaw`/`praticaNotesRaw`); l'ordinamento del popover (righe ~6138-6142, già su `h.date`) si allinea di conseguenza senza altre modifiche.
_Ultimo aggiornamento: 2026-07-15 (rev. 166) — rev.166: `admin.html`, tabella "File correnti" (screenshot utente: bottone "Reset" visibile solo scrollando orizzontalmente). **Causa**: `.actions-row{flex-wrap:nowrap}` + `td.actions{white-space:nowrap}` forzavano tutti i bottoni azione (Anteprima/Scarica/⋮/Reset) su un'unica riga senza mai andare a capo, e la tabella non aveva `colgroup` quindi "Origine" prendeva spazio extra in base al contenuto. Fix: (1) `.actions-row{flex-wrap:wrap}` + `td.actions{white-space:normal}` — i bottoni ora vanno a capo se non c'entrano; (2) aggiunto `colgroup` (File 20%, Sorgente 10%, Versioni 9%, Ultima modifica 15%, Origine 13%, Azioni 33%) per dare più spazio ad Azioni; (3) etichetta "Rigenerato automaticamente" abbreviata in "Rigenerato aut." nella cella (richiesta esplicita utente) — il tooltip al passaggio del mouse mostra comunque la dicitura estesa (nuova mappa `originTitleLabel`, separata da `originLabel` usata per il testo breve in tabella). `node --check` OK.
_Ultimo aggiornamento: 2026-07-15 (rev. 165) — rev.165: `server.py`, `GET /api/admin/pratiche-search` (tabella "Note pratiche") — richiesta utente: le tratte con `STATO_PERMESSO=OTTENUTO` non vanno mostrate (pratica emessa/chiusa, non ci si mette più note). Filtro aggiunto subito dopo quello su `PRATICA` vuota, prima del raggruppamento per pratica: `latest[latest["STATO_PERMESSO"].str.upper() != "OTTENUTO"]`. Il filtro è per singola tratta (non per l'intero gruppo pratica): se una pratica ha alcune tratte OTTENUTO e altre no, solo quelle ancora attive restano visibili/nell'elenco `tratta_ids` del gruppo.
_Ultimo aggiornamento: 2026-07-15 (rev. 164) — rev.164: `admin.html`, tab "Coda imprese" — bug segnalato da screenshot: filtrando per "Approvati" le card non erano in ordine di data. **Causa**: `refreshPending()` faceva solo `submissions.map(...)` sull'array cosi' come arrivava da `GET /api/admin/pending-updates`, senza alcun sort lato client (l'ordine dipendeva dall'ordine di inserimento in MongoDB, non dalla data mostrata in UI). Fix: aggiunto `.sort()` prima del render — ordina per `reviewed_at` se presente (rilevante per "Approvati"/"Rifiutati"), altrimenti `submitted_at` (per "In attesa"), decrescente (più recente in cima). Non toccato il backend: i timestamp sono già ISO-parsabili da `new Date()` (stessa funzione usata da `fmtDate`). `node --check` OK.
_Ultimo aggiornamento: 2026-07-15 (rev. 163) — rev.163: `imprese.html` + `mappa_impresa_caricamento.html`, **correzione di rev.162** su chiarimento esplicito dell'utente: NULLA OSTA NECESSARIO è un bisogno della **singola tratta selezionata**, non dell'intera pratica ("se seleziono quella tratta è perché quella tratta ha bisogno del nulla osta, non tutte"). rev.162 aveva capito il contrario (forzava "Aggiorna tutte insieme" a `checked` quando si spuntava il nulla osta). Invertito: spuntando NULLA OSTA NECESSARIO con tratte collegate presenti, il toggle **"Aggiorna tutte insieme"** (`siblingsToggle`/`prSibToggle`) viene ora forzato a **`false`** — l'aggiornamento resta di default sulla sola tratta corrente. L'impresa può comunque riattivare manualmente il flag se il nulla osta serve davvero per tutte le tratte della pratica ("salvo inserimenti dell'impresa diversi"). Nessun listener `change` esiste su `siblingsToggle`/`prSibToggle`, quindi impostare `.checked` via JS è sufficiente (letto solo al submit). `node --check` OK su entrambi i file.
_Ultimo aggiornamento: 2026-07-15 (rev. 162) — rev.162: `imprese.html` + `mappa_impresa_caricamento.html`, **rev.161 annullata/corretta** su indicazione esplicita dell'utente ("no era giusto prima"): il lock a livello di intero gruppo pratica introdotto in rev.161 (`_pratGroupNoNecLocked`/`_prGroupNoNecLocked`) è stato rimosso, tornando al lock **per singola tratta** di rev.160 (`SELECTED`/`PR_SELECTED['NULLA OSTA NECESSARIO']==='SI'`) sia nella selezione pratica, sia nel cambio stato, sia nel payload (si omette di nuovo il campo quando già SI, non si forza più SI esplicito). La reale soluzione al caso "stessa pratica, tratte diverse, checkbox visibile solo per alcune": quando l'utente spunta NULLA OSTA NECESSARIO su una tratta che ha tratte collegate, il checkbox **"Aggiorna tutte insieme"** (`siblingsToggle` in imprese.html, `prSibToggle` in mappa_impresa_caricamento.html) viene forzato automaticamente a checked nel listener `change` del checkbox nulla-osta — così il SI si propaga a tutte le tratte della pratica nello stesso salvataggio invece di restare sfasato su una sola. Rimossi gli helper di gruppo introdotti in rev.161 (dead code). `node --check` OK su entrambi i file.
_Ultimo aggiornamento: 2026-07-15 (rev. 161) — **SUPERSEDED da rev.162, vedi sopra**: lock a livello di intero gruppo pratica invece che di singola tratta, e invio SI esplicito sempre quando bloccato. Corretto perché la richiesta reale dell'utente era diversa (vedi rev.162). — rev.161: `imprese.html` + `mappa_impresa_caricamento.html`, fix su rev.160 segnalato dall'utente da screenshot: stessa pratica (`AUT/13/1A`), tratte diverse — su `TR_0383` compariva ancora il checkbox "NULLA OSTA NECESSARIO", su `TR_0444` no. **Causa**: rev.160 leggeva `NULLA OSTA NECESSARIO` solo dalla riga della tratta selezionata (`SELECTED`/`PR_SELECTED`), ma il campo è una decisione a livello di **pratica** (`PRATICA+ENTE+TIPO_PERMESSO`), non di singola tratta — dati storici salvati prima di questa regola potevano avere SI su alcune tratte del gruppo e NO/vuoto su altre. Fix: nuova `_pratGroupNoNecLocked()`/`_prGroupNoNecLocked()` — cerca in tutto l'array pratiche (`PRATICHE`/`PR_PRATICHE`) se **una qualsiasi** tratta dello stesso gruppo ha già SI; se sì, l'intera pratica è bloccata (checkbox nascosto per tutte le tratte del gruppo, non solo quella con SI). Cambiato anche il comportamento di invio: prima si **ometteva** il campo quando bloccato (il backend lo avrebbe comunque preservato copiando la riga precedente); ora si invia **SI esplicito** quando la pratica è bloccata (tranne skipChecks NO COMPETENZA/OTTENUTO) — così ogni tratta toccata da un futuro aggiornamento (singola o "aggiorna tutte insieme") viene automaticamente riallineata a SI, sanando nel tempo l'inconsistenza dei dati storici. `node --check` OK su entrambi i file.
_Ultimo aggiornamento: 2026-07-15 (rev. 160) — rev.160: `imprese.html` + `mappa_impresa_caricamento.html`, richiesta utente: **NULLA OSTA NECESSARIO diventa definitivo una volta impostato a SI** — non è più modificabile e il campo non deve più comparire per quella pratica negli aggiornamenti successivi. Applicato retroattivamente a tutte le pratiche esistenti senza toccare `Master.csv`: la logica legge il valore già presente sulla riga selezionata (`SELECTED`/`PR_SELECTED['NULLA OSTA NECESSARIO'] === 'SI'`) e se vero (1) nasconde il checkbox sia alla selezione della pratica sia ad ogni cambio di `STATO_PERMESSO` (prima si sarebbe riaperto passando da NO COMPETENZA/OTTENUTO a un altro stato — nuova var `noNecLocked`/`isAut && !noNecLocked` in entrambi i punti), (2) esclude il campo dal payload di submit (`noNec = (skipChecks || noNecAlreadySi) ? '' : ...`) — non inviandolo, il backend (`_apply_changes_to_df`, copia riga precedente + overwrite solo dei campi presenti in `fields`) lo mantiene automaticamente a SI in ogni nuova riga futura, senza bisogno di alcuna migrazione dati. Non toccato il tab "Nuova pratica" (`new-no-nec`/`pr-new-no-nec`): lì la pratica non esiste ancora, la scelta iniziale resta libera. `node --check` OK su entrambi i file.
_Ultimo aggiornamento: 2026-07-15 (rev. 159) — rev.159: `admin.html`, tabella "Note pratiche" (bug da screenshot utente: scrollando, la riga sotto l'header appariva "in doppia esposizione" sotto le intestazioni). **Causa reale**: `.note-table th{background:var(--surface2)}` e `.note-table tbody tr:hover td{background:var(--surface2)}` — `--surface2` non è mai stata definita in `:root` (esiste solo `--surface`, riga 29); una custom property non definita in `var()` senza fallback rende invalido il valore calcolato dell'intera dichiarazione, quindi il browser applica il valore iniziale di `background-color` (**trasparente**). L'header sticky era quindi trasparente da sempre in questa tabella (bug preesistente, mai notato finché la riga sotto non aveva contenuto abbastanza alto/multilinea da renderlo visibile — coincide con l'introduzione di `.stato-cell{white-space:normal}` in rev.158, che ha reso le righe più alte). Fix: entrambe le regole allineate a `var(--surface)` (bianco, già usata correttamente altrove nel file). Aggiunto anche ordinamento predefinito per `codice` all'apertura del tab (`NOTE_SORT` inizializzato a `{key:'codice',dir:1}` invece di `{key:null,dir:1}`) — coerente con l'ordine già prodotto dal backend, ma ora esplicito e con la freccia di sort attiva di conseguenza. `node --check` OK.
_Ultimo aggiornamento: 2026-07-15 (rev. 158) — rev.158: `admin.html`, tabella "Note pratiche" (feedback utente da screenshot dopo rev.156/157). (1) Rimossa colonna "Tratte" (non serve più a vista, l'elenco tratte resta comunque nel form "+ Nota" per conferma prima del salvataggio) — colgroup 7→6 colonne, colspan placeholder 7→6 in 4 punti. (2) Colonna "Stato" non era leggibile per intero (`.note-table td{...text-overflow:ellipsis;white-space:nowrap}` generico troncava "IN REDAZIONE INTEGRAZIONE" ecc.) — nuova classe `.stato-cell{white-space:normal;word-break:break-word}` (stesso pattern già usato per `.note-cell`), colonna allargata 12%→20%. (3) Colonna "Nota attuale" ridotta 27%→24% per compensare. Nuove percentuali colgroup: Codice 14, Ente 20, Lotto 8, Stato 20, Nota 24, Azioni 14. (4) **Colori stato allineati al kit brandkit** (richiesta esplicita utente, "i colori devono sempre rispettare il kit aziendale"): prima il testo stato era senza colore/mapping in questa tabella; aggiunta `NOTE_STATO_COLORS` — copia esatta della palette canonica in AGENT_BRIEF §8bis (solo variabili `--gray-500`/`--warn`/`--retelit-blue`/`--retelit-sky`/`--err`/`--retelit-blue-75`/`--ok`, nessun hex arbitrario), applicata inline sulla cella stato via `_noteStatoColor()`. `node --check` OK sugli script inline.
_Ultimo aggiornamento: 2026-07-15 (rev. 157) — rev.157: `server.py`, `GET /api/admin/pratiche-search` (tabella "Note pratiche" di `admin.html`, rev.156) — richiesta utente: le righe senza `PRATICA` valorizzata non vanno mostrate affatto (non possono comunque ricevere una nota via `POST /api/admin/pratiche/note`, che richiede un numero pratica). Rimosso il fallback "raggruppa per singola tratta" introdotto in rev.156 per queste righe: ora vengono filtrate via prima del raggruppamento (`latest[latest["PRATICA"]!=""]`). Nessun impatto sulle pratiche con `PRATICA` valorizzata (comportamento rev.156 invariato).
_Ultimo aggiornamento: 2026-07-15 (rev. 156) — rev.156: **admin.html + server.py**, 2 richieste utente da screenshot (tab "Note pratiche" vs "File correnti"). (1) **fix layout**: `.grid{grid-template-columns:1.05fr 1.95fr}` (riga 55) non aveva `minmax(0,...)` → la larghezza reale delle due colonne dipendeva dal min-content del pannello **attivo**, non dalla proporzione fissa 1.05/1.95: la tabella "File correnti" (layout auto, bottoni Anteprima/Scarica/⋮ non wrappabili) ha un min-content alto e allargava la traccia; gli altri pannelli (specialmente "Note pratiche", `table-layout:fixed`) hanno min-content basso e la traccia si restringeva, dando l'impressione di "table che si allarga/restringe cambiando tab". Fix: `grid-template-columns:minmax(0,1.05fr) minmax(0,1.95fr)` (blocca la proporzione sempre alla stessa larghezza) + `overflow-x:auto` su `.card` come rete di sicurezza se un contenuto interno resta comunque più largo della colonna. (2) **Note pratiche — raggruppamento per pratica**: prima una riga per `TRATTA_ID`, ora una riga per `PRATICA+ENTE+TIPO_PERMESSO` (stesso concetto di `PR_SIBLINGS` in `mappa_impresa_caricamento.html`), con colonna "Tratte" che elenca tutte le tratte del gruppo. Se `PRATICA` è vuota la riga resta per singola tratta (non c'è chiave sicura per raggruppare). Backend: `GET /api/admin/pratiche-search` — dedup latest per tratta come prima, poi raggruppamento per pratica (`_gkey_row`); riga rappresentativa = quella con `NOTE` più recente non vuota, altrimenti la prima; nuovo campo `tratta_ids` (lista) oltre a `tratta_id` (compat, primo della lista) e `n_tratte`. `POST /api/admin/pratiche/note` — accetta ora `tratta_ids` (lista, nuovo) oltre a `tratta_id` (singolo, retrocompatibile), costruisce un `change` per tratta e scrive **tutte in un'unica versione Master.csv** (stesso `submission.changes` array già supportato da `_apply_changes_to_df`, nessuna modifica lì). Frontend: ricerca/sort/render aggiornati su `tratta_ids`; form nota mostra l'elenco completo delle tratte coinvolte; submit invia `tratta_ids` invece del singolo `tratta_id` → la nota si applica a tutta la pratica in un solo salvataggio, come già avviene per stato/altri campi lato imprese.
_Ultimo aggiornamento: 2026-07-08 (rev. 155) — rev.155: **fix bug** `index.html`, popup "Storico Stato" (righe ~6129-6139) — segnalato dall'utente da screenshot: quando due cambi stato ricadono sulla stessa data (es. INVIATO→PROTOCOLLATO nello stesso giorno), l'ordine mostrato poteva risultare invertito (INVIATO sopra PROTOCOLLATO). Causa: `praticaStatoRaw` (righe ~4091-4136) accumula le voci nell'ordine delle righe del CSV grezzo, non nell'ordine cronologico reale; il sort del popover confrontava solo la data (granularità giorno, senza ora) e a parità di data il sort stabile manteneva l'ordine "casuale" del CSV. Fix: a parità di data, tie-breaker sull'indice della pipeline stati già esistente (`STATI`, riga 2133 — `...INVIATO, PROTOCOLLATO...`), così lo stato più avanzato nella pipeline appare come più recente. Non copre il caso limite (raro) di due voci con stesso stato e stessa data: in quel caso resta l'ordine del CSV, nessun impatto visibile (stato identico).

_Ultimo aggiornamento: 2026-07-07 (rev. 154) — rev.154: `gantt.html` — dipendenze reali, estratte dagli screenshot del `.mpp` originale forniti dall'utente. Nuova mappa `GANTT_DEPS` (id riga successore → {pred, type FS/SS, lagDays, approx}): Consegna materiali 1/2/3 tranche ← Richiesta forniture (FS +10/+30/+70g); Test/Collaudo per comune ← Opere Civili stesso comune (SS, **approssimato**: nel .mpp il predecessore reale era la sotto-attività "TOC" interna a Opere Civili — qui non modellata come riga separata, quindi agganciata alla barra aggregata più vicina, segnalato con `approx:true`); As-built per comune ← Test/Collaudo stesso comune (FS, esatto — 1:1 col .mpp). Nessuna dipendenza per Fase 2 (permessi): nel .mpp la colonna Predecessori era vuota per tutti gli enti. Connettori disegnati come path SVG (elbow + freccia) tra le righe, solo se entrambe visibili (spariscono se una fase è collassata); tratteggio grigio per le dipendenze approssimate, teal continuo per quelle esatte. Click su un task (qualunque ruolo, fuori da "Modalità modifica") apre finestra di sola lettura con predecessore/successori e tipo di relazione; in "Modalità modifica" (admin) la stessa sezione appare in fondo al popover di editing. Rimosso anche, su richiesta utente, il testo " · dato reale Master.csv"/" · baseline pianificazione" dal tooltip hover delle milestone. div bilanciati e `node --check` OK.
_Ultimo aggiornamento: 2026-07-07 (rev. 153) — rev.153: `gantt.html` — richiesta utente: rimosso " · dato reale Master.csv"/" · baseline pianificazione" dal tooltip hover delle milestone (`tipMs`, riga ~501); il flag `row.real` resta usato altrove (colore/spessore contorno del rombo) ma non più mostrato come testo. Richiesta collegata (popup click-per-vedere-dipendenze predecessore/successore di un task) **non implementata**: nessuna fonte dati esiste per le dipendenze (vedi rev.151) — in attesa di input utente su come fornirle prima di scrivere qualunque UI.
_Ultimo aggiornamento: 2026-07-07 (rev. 152) — rev.152: **hub.html** — richiesta utente: spostare la card "Pannello Gestione" (`adminCard`, sezione ADMIN, prima da sola in fondo alla pagina) nella stessa riga di Alert Predittivi + Gantt (flex-row introdotta in rev.150). Ordine finale nella riga: Alert Predittivi → Pannello Gestione → Gantt, tutte `flex:1 1 340px; max-width:360px`. Nessun cambio alla logica JS di visibilità admin-only (righe ~686-691, stessi `getElementById`/`style.display`), solo riposizionamento del markup. div bilanciati verificato, id univoci (`adminCard`/`ganttCard`/`impresaCardsWrap`) verificato.
_Ultimo aggiornamento: 2026-07-07 (rev. 151) — rev.151: **gantt.html**, feedback utente su rev.149/150 (3 punti). (1) Le milestone (righe `ms`, permessi) mostravano solo la data, senza %: aggiunta `· NN%` accanto alla data per ogni milestone, stesso trattamento delle barre (che la % ce l'avevano già). (2) Aggiunto collassa/espandi per fase: click sull'header "FASE N · ..." (rettangolo+chevron+testo, tutti con `data-group-id`) nasconde/mostra le righe della fase (stato in `COLLAPSED_GROUPS`, `Set` di `row.id` delle righe `g`); quando collassata mostra "(N task nascosti)" nell'header. Funziona sempre, indipendente da "Modalità modifica". `totalH`/`rowY` ricalcolati escludendo le righe nascoste. (3) **Chiarimento, non bug**: le dipendenze tra task (frecce predecessore→successore) non sono mai state presenti in questa pagina — `GANTT_ROWS` (baseline statica estratta una tantum dal `.mpp`, vedi rev.0 di questa pagina) non contiene alcun campo di dipendenza, solo `start`/`end`/`date`/`pct`. Non è quindi una regressione di questa sessione: per averle serve (a) riestrarre i predecessori dal `.mpp` originale (non disponibile qui) oppure farseli specificare a mano riga per riga, e (b) disegnarli come connettori SVG tra barre/milestone, editabili in "Modalità modifica" — feature non ancora scoping-ata, in attesa di conferma utente su priorità/fonte dati.
_Ultimo aggiornamento: 2026-07-07 (rev. 150) — rev.150: **hub.html — correzione layout** (feedback utente su rev.149: "doveva essere sulla stessa riga di AI Predittivi", non stacked sotto Milestone). Rimosso `.card-stack`/wrapping Milestone+Gantt introdotto in rev.149: Milestone torna una card normale della `.cards-grid` a 3 colonne. Gantt spostato fuori dalla grid, in un nuovo flex-row insieme ad "Alert Predittivi" (che prima era da solo, `max-width:360px`) — ora entrambi `flex:1 1 340px; max-width:360px`, stessa riga, si adattano affiancati o impilati sotto 680px circa. `ganttCard` resta `id` invariato, stessa logica JS di visibilità admin-only (riga ~687), nessun cambio lì. div bilanciati verificato.
_Ultimo aggiornamento: 2026-07-07 (rev. 149) — rev.149: **hub.html + gantt.html + server.py**, richiesta utente (2 punti). (1) `hub.html`: card Gantt appariva sotto Polizze invece che sotto Milestone — causa: grid 3 colonne, Gantt era il 7° item visibile → colonna 1 (sotto Polizze), non colonna 3 (sotto Milestone, 6° item). Fix: Milestone+Gantt raggruppate in nuovo `.card-stack` (flex-column, singola cella della grid) → Gantt sempre sotto Milestone a ogni breakpoint, nessuna dipendenza dal conteggio item. (2) `gantt.html`: **bug trovato** — `mergeMasterCsv()` chiamava `fetch('Master.csv')` senza header `x-session-token` (unica pagina rimasta scoperta dal fix rev.135, che aveva coperto index/mappa/mappa_impresa_caricamento/polizze_convenzioni/admin ma non gantt — coerente con TODO §11.10 mai chiuso); con backend attivo il 401 veniva silenziato dal `catch`, quindi le milestone Fase 2 (permessi) restavano bloccate a 0% per sempre, a prescindere dallo stato reale su Master.csv. Aggiunto header mancante. Nuova modalità "modifica" (solo admin/admin2, bottone in `section-head`, icona SVG coerente col brand kit — niente unicode/emoji): click su barra/milestone in modalità attiva apre popover HTML con % completamento, date, note — editabile per qualunque riga, incluse quelle senza invio impresa (materiali, cantierizzazione, opere civili). Persistenza: `server.py` — nuova collection Mongo `gantt_overrides` (chiave `lotto+row_id`, `row_id`=indice statico in `GANTT_ROWS`), nuova dependency `_require_admin_session` (staff + ruolo admin/admin2, per le sole scritture), endpoint `GET/PUT/DELETE /api/gantt/overrides/...`. Un override manuale vince sempre su baseline e su Master.csv (applicato dopo `mergeMasterCsv()` nel flusso di load). "Ripristina" ricarica la pagina (evita di dover tenere una copia della baseline in memoria). Integrazione fatta sulla versione più recente di `server.py` (quella con `_master_csv_lock`/backfill di rev.147-148): nessuna sovrapposizione, endpoint Gantt aggiunti in coda al file, `py_compile` OK. TODO §11.10 (pagina Gantt) da considerarsi chiuso lato bug isolamento auth; resta aperta solo l'estensione multi-lotto (oggi la pagina è hardcoded su Lotto 1A). Nota: le prime due volte questo lavoro era stato numerato per errore rev.147/148 (collisione con la numerazione reale, fatta in un'altra sessione senza visibilità su questo brief) — corretto qui a rev.149.
_Ultimo aggiornamento: 2026-07-07 (rev. 148) — rev.148: aggiunto endpoint one-off `POST /api/admin/backfill-data-update-solleciti` (`x-upload-token`) per popolare `DATA_UPDATE` sulle tratte con solleciti registrati **prima** del fix rev.147 (touch mai avvenuto per il bug di mask). Logica: per ogni `tratta_id` in `solleciti`, prende la `data_sollecito` più recente e la scrive in `DATA_UPDATE` solo se più recente di quella già presente (idempotente, riesguibile senza rischio, non tocca tratte senza solleciti). Da lanciare una tantum dopo il deploy di rev.147, poi ignorabile (endpoint resta nel codice ma innocuo se rilanciato).
_Ultimo aggiornamento: 2026-07-07 (rev. 147) — rev.147: **fix bug** — `DATA_UPDATE` non si aggiornava mai registrando un sollecito (segnalato dall'utente da screenshot, colonna "Update" sempre "–"). Causa: `_touch_data_update`/`_touch_data_update_multi` (server.py) filtravano la riga Master.csv anche su `PRATICA==pratica`, ma `imprese.html` invia come `pratica` il codice composito `_codice` (es. `"AUT/1/1A"`, prefix/PRATICA/lotto — vedi `buildCodice()`), non il valore grezzo della colonna `PRATICA` nel CSV (es. `"1"`) — mask non trovava mai match, funzione ritornava silenziosamente senza scrivere né loggare errori. Fix: rimosso il filtro extra su `pratica`/`tipo_permesso`, match solo su `TRATTA_ID` (già usato come chiave sufficiente altrove nel file, es. righe 1561/1602/2425). Il campo `pratica` resta salvato nel record Mongo del sollecito (per display), solo non più usato per il filtro Master.csv.
_Ultimo aggiornamento: 2026-07-07 (rev. 146) — rev.146: verificato `server.py` (upload utente) — TODO rev.145 chiuso, nessun fix necessario. `impresa_submit` (riga 1416) non valida `fields`, nessuno schema richiede `STATO_PERMESSO`. `_apply_changes_to_df` (riga 1584): `DATA_ULTIMA_MODIFICA` auto-set solo `if "STATO_PERMESSO" in fields` (riga 1621, corretto — nota non deve toccarla); `DATA_UPDATE` invece sempre stampata se assente da `fields` (riga 1624), commento nel codice già anticipava esplicitamente il caso "sola nota". Path frontend rev.145 → backend end-to-end ok.
_Ultimo aggiornamento: 2026-07-07 (rev. 145) — rev.145: **frontend** `imprese.html` + `mappa_impresa_caricamento.html` — richiesta utente: un'impresa deve poter salvare un aggiornamento di sola nota senza essere obbligata a cambiare `STATO_PERMESSO` (l'iter/le transizioni di stato non cambiano, serve solo poter toccare `DATA_UPDATE` anche via nota). `imprese.html`: campo `fld-note` non più disabilitato quando `fld-stato` è vuoto; `addToQueueBtn` ora richiede stato **o** nota (non più solo stato), `fields` costruito senza `STATO_PERMESSO` se assente, `skipChecks` (polizza/convenzione/nulla osta) esteso a `!stato` oltre a NO COMPETENZA/OTTENUTO. `mappa_impresa_caricamento.html` (`prAddToQueue`): stessa modifica su `pr-note`; il guard esisteva già in forma parziale (permetteva submit con sola `DATA_PREVISTA_RILASCIO` senza stato) — esteso per accettare anche sola nota. TODO backend verificato e chiuso in rev.146.
_Ultimo aggiornamento: 2026-07-07 (rev. 143) — rev.143: `imprese.html`, tabella "Nuovo sollecito — seleziona la pratica" (`renderSolPratiche`, riga ~1204) — aggiunta colonna "Data ultimo sollecito" (colspan 5→6), stessa logica di rev.142: max `data_sollecito` tra `SOLLECITI` filtrati sui `TRATTA_ID` del gruppo pratica. Fix collaterale: `loadSolleciti()` chiamava `renderSolPratiche('')` in parallelo (non atteso) prima che `SOLLECITI` fosse popolato — ora `renderSolPratiche` viene richiamata anche a fine `loadSolleciti()`, preservando il filtro di ricerca attivo.
_Ultimo aggiornamento: 2026-07-07 (rev. 142) — rev.142: `imprese.html`, tabella Solleciti (riga ~385) — chiuso il TODO lasciato in rev.141: rimossa colonna "Data ottenimento" (`p?.DATA_APPROVAZIONE`, stessa colonna morta), sostituita con "Data ultimo sollecito" — calcolata client-side in `renderSolleciti()` come max di `data_sollecito` tra tutti i record `SOLLECITI` con lo stesso `tratta_id` (usa `parseDate()` esistente per il confronto gg/mm/aaaa). Nessun cambio a `colspan`/backend: la colonna esisteva già, solo rinominata e ricalcolata.
_Ultimo aggiornamento: 2026-07-07 (rev. 141) — rev.141: `imprese.html`, tabella "Aggiorna pratiche" — rimossa colonna "Data ottenimento" (`DATA_APPROVAZIONE`, sempre `—` perché le pratiche `OTTENUTO` sono filtrate a monte dalla vista, righe 590-592). "Data agg." (`DATA_ULTIMA_MODIFICA`) resta e copre già l'esigenza di vedere l'ultimo aggiornamento. Colonne 9→8, colspan aggiornati sui 3 placeholder (loading/errore/vuoto). Stessa colonna morta presente anche nella tabella Solleciti (riga ~385) — non ancora rimossa, in attesa di conferma.
_Ultimo aggiornamento: 2026-07-07 (rev. 140) — rev.140: **fix isolamento dati** — `GET /api/imprese/cantieri/{key}/log` non verificava ownership sul lotto (a differenza di `update_cantiere` sullo stesso path): qualsiasi impresa autenticata poteva leggere lo storico giornaliero (stato, metri, note, motivo blocco) di cantieri di altre imprese conoscendo/indovinando `cantiere_key`. Fix: stesso controllo `_find_assignment`+`_lotto_from_source` di `update_cantiere` → 403 se il lotto del cantiere non è tra quelli assegnati. Trovato durante audit di follow-up post rev.139 (richiesta utente: "un'impresa non può prendere dati di altre imprese?"). Verificati in stessa sessione tutti gli altri endpoint `/api/imprese/*`: scoping OK su `pratiche`/`master-sed`/`solleciti` GET/`cantieri` GET/`my-submissions`; ownership per-record OK su tutte le delete. Nota minore aperta, non bloccante: `add_sollecito`/`bulk-insert` non validano che `tratta_id` appartenga ai lotti dell'impresa (scrittura, non lettura — il record resta comunque taggato col `nome` reale dal token, nessuna falsificazione d'autore).
_Ultimo aggiornamento: 2026-07-07 (rev. 139) — rev.139: **fix sicurezza — leak Master/QGIS/SED/Riepilogo verso ruolo impresa**. Falla: `/api/data/{filename}`+`/api/data-text/{filename}` erano dietro `_require_session` (rev.135) ma senza controllo ruolo — un token impresa legittimo poteva scaricare via curl il dataset intero di tutti i lotti/concorrenti (`Master.csv`, `QGIS.geojson`, `Riepilogo_progettazione.csv`, `SED_classificato.geojson`), bypassando lo scoping delle pagine `/api/imprese/*`. `mappa_impresa_caricamento.html` inoltre fetchava `QGIS`/`SED`/`Master` interi via `fetchWithSWR`/`_staticFileUrl`, in parte anche dalla gamba statica GitHub Pages (nessun token). server.py: nuovo set `SENSITIVE_FILES` (i 4 file sopra) + `_guard_sensitive_read(rel, sess)` → 403 se `sess.ruolo=='impresa'`, chiamata in `get_data_file`/`get_data_text`; nuovo endpoint scoped `GET /api/imprese/master-sed` (pattern `impresa_pratiche`/`_find_assignment`+`_lotto_from_source`) che ritorna `{qgis, sed, lotti}` filtrati server-side su `properties.LOTTO` dei lotti assegnati; `_push_to_github` ora fa return immediato se il basename è in `SENSITIVE_FILES` (i 4 file non vengono più pubblicati sul repo pubblico — derivati restano su GridFS, autoritativo). `mappa_impresa_caricamento.html`: nuova `_loadScopedGeo()` (cache in-memory sessione) che sostituisce i 3 fetch diretti (load QGIS, layer SED, export SED) — nessun fetch residuo di file interi, verificato via grep. Verificato: `py_compile` OK su `server.py`, `node --check` OK su tutti gli script inline del frontend; `imprese.html`/`imprese_scavi.html` non a rischio (fetchavano solo `/api/imprese/*`, mai toccato file core). ⏳ **TODO operativo (non codice, richiede intervento manuale in produzione)**: `SED_classificato.geojson` non ha ancora versione su GridFS (solo seed su disco/git) — va caricato una tantum da `admin.html` PRIMA di rimuoverlo da git, altrimenti `/api/data/SED...` va 404. Ordine deploy: (1) seed SED su Mongo via admin.html, verificare `GET /api/files` → `source:mongo`; (2) deploy `server.py`+`mappa_impresa_caricamento.html`; (3) `git rm` dei 4 file dal repo pubblico; (4) purge history separata (git filter-repo/BFG), finestra dedicata — TODO rev.135 sul repo pubblico resta aperto fino a questo passo.
_Ultimo aggiornamento: 2026-07-06 (rev. 138) — rev.138: **fix isolamento dati** — `GET /api/imprese/solleciti` (server.py) filtrava le tratte del lotto con `Source.Name.str.contains(lotto)` (substring), non match esatto come `_lotto_from_source()` usato ovunque altro: un'impresa su "Lotto 2" poteva vedere anche solleciti di "Lotto 2A" se assegnato ad altra impresa. Fix: stesso criterio esatto di `impresa_pratiche`/`get_cantieri_impresa` (`_lotto_from_source(x) in lotti`, `lotti` come set). Fallback su `{"impresa": nome}` invariato.
_Ultimo aggiornamento: 2026-07-06 (rev. 137) — rev.137: **pulizia documentale + audit pre-produzione** (imprese vanno live da domani). Doc: rimossi tutti i riferimenti residui a `mappa_impresa.html` (rimossa rev.126, non più nel repo) da §3/§4/§5.12/§6/§7.1/§8bis/§8.23/§8.30; corretta tabella §5.1 — Auth di `/api/files`, `/api/data*`, `/api/preview`, `GET /api/uploads` segnava ancora "—" (pubblico) mentre il codice li protegge con `_require_session`/`_require_staff_session` da rev.135 (verificato via grep su `backend/server.py`). Audit sulle 3 pagine impresa (`imprese.html`, `imprese_scavi.html`, `mappa_impresa_caricamento.html`): `node --check` su tutti gli script inline (nessun errore di sintassi); verificati tutti gli handler `onclick`/`onchange` contro scope IIFE (nessun caso analogo al bug rev.116/118, `_openHelp`/`_retryBoot` restano correttamente esposti su `window`); verificata coerenza chiave `'POLIZZA '` (spazio finale, combacia con l'header reale di `Master.csv`) tra `imprese.html`/`mappa_impresa_caricamento.html`/`server.py`; verificata logica `skipChecks` (NO COMPETENZA/OTTENUTO nascondono Nulla Osta/Polizza/Convenzione) coerente nei due file; verificato l'endpoint `POST /api/imprese/cantieri/{key}` (accumulo `metri_scavati` via `$inc`, validazione `metri_realizzati_oggi` non negativo, cap sui metri rimanenti) — nessun bug bloccante trovato. Verifica successiva di isolamento dati cross-impresa su tutti gli endpoint `/api/imprese/*`: trovato bug in `get_solleciti`, fixato in rev.138. Note minori non bloccanti, lasciate aperte: (a) `imprese_scavi.html` — se l'utente forza un valore negativo in `#fMetri` (bypassando `min="0"` dell'input), il valore viene silenziosamente scartato invece di mostrare un errore esplicito; (b) `mappa_impresa_caricamento.html` r.2966 — `fetch('/api/imprese/me?nome=...')` passa ancora `?nome=` in query, ignorato server-side (stesso pattern già noto in `hub.html`, §7.1); possibile race condition teorica in `update_cantiere` se due submit sullo stesso cantiere arrivano in parallelo (lettura `rimanenti` non atomica rispetto all'`$inc` successivo) — rischio basso, un solo utente/impresa scrive per cantiere.
_Ultimo aggiornamento: 2026-07-06 (rev. 136) — rev.136: verifica TODO §11 su repo completo (zip caricato). Chiusi 2: (1) verifica pendente rev.129/130 — `mappa.html` non chiama `/api/lotti-cantieri`/`/api/sopralluoghi`, `sopralluoghi.html` già ok su tutte e 4 le fetch; (2) export GeoJSON lotti impresa — `exportGeoJSONLotti()` già presente/funzionante da rev.78-80. Confermato ancora aperto 11.4 (solleciti non più uniti alle note dopo rimozione rev.70). Non verificabili via grep (dati, non codice): 11.6 (data invio NO/27/1A), 10 (pagina Gantt), 6.1/6.3/6.4 (imprese_scavi).
_Ultimo aggiornamento: 2026-07-06 (rev. 135) — rev.135: **fix sicurezza** — `/api/data/{filename}`, `/api/data-text/{filename}`, `/api/files`, `/api/preview/{filename}`, `GET /api/uploads` erano completamente senza autenticazione: chiunque poteva scaricare Master.csv/QGIS.geojson/Riepilogo/ecc. direttamente dal backend Render, bypassando login e ruoli (segnalato dall'utente: timore che le imprese trovassero accesso ai file). `server.py`: `SESSION_SECRET`/`_verify_session`/`_require_session`/`_require_staff_session` spostati PRIMA delle routes (erano dopo — causava NameError a import time usandoli come `Depends()` in endpoint precedenti). `/api/data`+`/api/data-text` → `Depends(_require_session)` (letti anche da imprese via `mappa_impresa_caricamento.html`); `/api/files`+`/api/preview`+`GET /api/uploads` → `Depends(_require_staff_session)` (solo staff, admin.html). ⚠️ **Gap rimasto aperto da qui, chiuso in rev.139**: `_require_session` valida solo che il token sia valido, non il ruolo — un token impresa poteva comunque leggere `/api/data/Master.csv` (e QGIS/SED/Riepilogo) per intero. Frontend aggiornato per inviare `x-session-token` sulle chiamate dati finora "nude": `index.html`/`mappa.html`/`mappa_impresa_caricamento.html` (fetch `rel+qs` riscritto da `api-config.js` verso `/api/data/...`), `polizze_convenzioni.html` (`/api/data-text/Master.csv`), `admin.html` (wrapper `api()` ora inietta l'header di default per tutte le chiamate, incluse `/api/files`/`/api/preview`). ⚠️ Non toccato: fetch `_staticFileUrl(...)` verso GitHub Pages diretto (bypassano il backend, nessun auth necessario finché i CSV restano committati su git — vedi TODO sotto) e `loadSolleciti()` in `index.html` (unico fetch di solleciti.csv, solo statico GitHub, nessun fallback backend — se si toglie il file da git va aggiunto un fallback come per Master/Riepilogo). ⏳ TODO aperto: repo GitHub `ENRI-RDS/dashboard` è pubblico — i CSV/geojson restano scaricabili da `raw.githubusercontent.com` anche dopo questo fix e anche se rimossi dai commit futuri (restano nella storia). Valutare: smettere di pushare i derivati su git + purge storia (git filter-repo/BFG) o rendere il repo privato.
_Ultimo aggiornamento: 2026-07-06 (rev. 134) — rev.134: `server.py` — fix `delete_upload` (`DELETE /api/uploads/{id}`): pushava su GitHub + rigenerava `QGIS.geojson`/`Riepilogo_progettazione.csv` ad OGNI cancellazione di una versione con `filename==Master.csv`, anche cancellando vecchie versioni non correnti (nessun cambio di contenuto, solo commit/rigenerazione ridondanti — segnalato dall'utente: "cancellavo vecchi master e vedevo nuovi file propagarsi"). Fix: prima della cancellazione, `_current_upload(MASTER_FILENAME)` determina se il doc cancellato era la versione corrente; push/rigenerazione ora condizionati a `was_current`. `delete_file` (cancellazione totale versioni) non toccato: lì il fallback cambia davvero, il push resta corretto.
_Ultimo aggiornamento: 2026-07-06 (rev. 133) — rev.133: audit log azioni admin. Nuova collection `admin_actions_col` + helper `_log_admin_action(azione, target, actor_nome)`, agganciato a upload/delete_version/delete_all_versions/restore/prune_versions. `actor_nome` letto da header `x-actor-nome` (auto-dichiarato dal client, non verificabile — sufficiente per il tracciamento voluto, non per prova legale). `admin.html`: `authHeaders()` ora invia `x-actor-nome` da `localStorage._enri_user`; fetch upload (diversa, usa FormData) aggiornata a parte. Nuovo endpoint `GET /api/admin/actions` (stesso `UPLOAD_TOKEN`) per consultare il log.
_Ultimo aggiornamento: 2026-07-06 (rev. 132) — rev.132: `milestone.html` — chiuso gap auth (era l'ultimo aperto da rev.26/rev.131). Guardia client-side identica pattern scavi.html: redirect a `hub.html` se `_enri_user` assente o `_enri_role` non in `['admin','admin2','user']` (ruoli consentiti, tutti tranne impresa). Gap auth §11 ora chiuso su tutte le pagine note.
_Ultimo aggiornamento: 2026-07-06 (rev. 131) — rev.131: `admin.html` — chiuso (parzialmente) il gap auth noto da rev.26 (§11): nessuna guardia login/ruolo, pagina raggiungibile da chiunque abbia l'URL (azioni comunque bloccate da `UPLOAD_TOKEN` lato server, ma UI/lista file/versioni erano pubbliche). Aggiunta guardia client-side a inizio script: redirect a `hub.html` se `_enri_user` assente o `_enri_role` non in `['admin','admin2']`. È solo client-side (bypassabile settando localStorage a mano), non un vero controllo server — la vera barriera per le scritture resta `UPLOAD_TOKEN`. ⏳ `milestone.html` — stesso gap, non ancora verificato/corretto in questa sessione.
_Ultimo aggiornamento: 2026-07-06 (rev. 130) — rev.130: chiuso TODO rev.129. `sopralluoghi.html` — già inviava `x-session-token` su tutte e 4 le fetch verso i 3 endpoint gated (`/api/lotti-cantieri`, `GET/POST/DELETE /api/sopralluoghi*`), nessuna modifica. `mappa.html` — fetch `/api/cantieri` (r.2619, `_loadCantieri` o simile) senza header → aggiunta. Verifica completata su tutti i file caricati finora; `mappa_impresa_caricamento.html`/`imprese.html`/`imprese_scavi.html` non a rischio per costruzione (solo `/api/imprese/*`), non ricontrollati via grep in questa sessione.
_Ultimo aggiornamento: 2026-07-06 (rev. 129) — rev.129: **falla di autorizzazione lato server** segnalata dall'utente in vista rilascio credenziali imprese ("uno smanettone potrebbe evitare le pw e accedere a tutto"). Causa reale: `server.py` — quasi tutti gli endpoint non-`/api/imprese/*` erano protetti solo da `_require_session` (token valido, qualsiasi ruolo), non da un controllo ruolo — un account `impresa` autenticato poteva chiamare direttamente (curl/devtools) endpoint pensati solo per staff, bypassando l'hub e le pagine che nascondono le card lato client. `/api/admin/*` è ok (protetto da `UPLOAD_TOKEN` separato, un impresa non lo conosce); `/api/enti`, `/api/logs/*` sono voluti anche per impresa (usati da `mappa_impresa_caricamento.html`). Fix: nuova dependency `_require_staff_session` (= `_require_session` + 403 se `ruolo=='impresa'`), applicata a 6 endpoint: `GET /api/lotti-cantieri`, `GET/POST /api/sopralluoghi`, `GET /api/sopralluoghi/next-codice`, `GET /api/cantieri`, `GET /api/cantieri/{key}/log` (questi ultimi due erano volutamente pubblici, ora gated su richiesta utente — "scavi deve essere come index, tutte tranne quelle impresa"). Conseguente fix in `scavi.html`: due fetch (`_loadCantieri` r.739, registro cantiere r.936) non inviavano `x-session-token` → aggiornate per non rompersi con 401 dopo il deploy del backend. ⏳ **TODO verifica non completata in questa sessione** (file non caricati): controllare se `mappa.html`, `sopralluoghi.html` chiamano `/api/lotti-cantieri` o `/api/sopralluoghi` senza header sessione — stesso rischio di rottura silenziosa. `mappa_impresa_caricamento.html` non a rischio per costruzione (usa solo `/api/imprese/*` scoped), non riverificato via grep in questa sessione.
_Ultimo aggiornamento: 2026-07-04 (rev. 128) — rev.128: `imprese_scavi.html` — rimosso tasto topbar "Mappa" → `mappa_impresa.html` (eliminata), stesso fix rev.127. Nessun altro riferimento residuo. TODO §rev.127 chiuso, tutte e 3 le pagine impresa allineate.
_Ultimo aggiornamento: 2026-07-04 (rev. 127) — rev.127: rimosso tasto topbar "Mappa" → `mappa_impresa.html` (eliminata, rev.126) da `imprese.html` e `mappa_impresa_caricamento.html`, nessun codice JS morto associato (era solo `<a href>`). ⏳ TODO: applicare stesso fix a `imprese_scavi.html` (3ª pagina impresa, non ancora caricata in questa sessione — verificare se ha lo stesso link).
_Ultimo aggiornamento: 2026-07-04 (rev. 126) — rev.126: `hub.html` — rimossa card "Mappa Impresa" (`impresaMapCard` → `mappa_impresa.html`), ridondante: `mappa_impresa_caricamento.html` è già un superset (stesso viewer Leaflet/filtri/sidebar/ricerca + form aggiornamento; unica feature esclusiva persa era `exportPDF()`, non richiesta). Rimosso anche il riferimento `impresaMapCard` dalla logica JS di visibilità card per ruolo. **Da fare**: eliminare fisicamente il file `mappa_impresa.html` dal repo/deploy (non gestito da questa sessione, solo hub.html aggiornato).
_Ultimo aggiornamento: 2026-07-04 (rev. 125) — rev.125: `mappa_impresa_caricamento.html` — allineamento a imprese.html + fix da screenshot utente: (1) `OTTENUTO` come `NO COMPETENZA` nasconde/resetta/esclude Nulla Osta/Polizza/Convenzione (submit `skipChecks`), come rev.124; checkbox già stacked verticalmente qui (form mobile single-column, non grid2, nessun disordine da correggere); (2) rimosso hint "Stima fornita dall'impresa..." sotto Data prevista rilascio; (3) sottotitolo "Aggiornamento — AUT/24/1A · TR_0451 · COMUNE DI PERO · AUTORIZZAZIONE" (tutto mono su riga unica, illeggibile su mobile) ristrutturato su 2 righe: riga1 codice bold accent + tratta mono, riga2 ente + tipo permesso come badge pillola (nuova classe `.pr-edit-sub`, popolata via innerHTML invece di textContent).
_Ultimo aggiornamento: 2026-07-04 (rev. 124) — rev.124: `imprese.html` — stato `OTTENUTO` ora si comporta come `NO COMPETENZA` per `wrap-checkboxes` (Nulla Osta/Polizza/Convenzione): nascosti, reset, esclusi dal payload (`skipChecks`). Data prevista rilascio era già esclusa per OTTENUTO da prima (rev.44), non toccata.
_Ultimo aggiornamento: 2026-07-04 (rev. 123) — rev.123: `imprese.html` — (1) fix layout tab Aggiorna: le 3 checkbox (Nulla Osta/Polizza/Convenzione), introdotte in rev.120 sparse nella grid2 causando disordine visivo (screenshot utente), ora raggruppate in un unico blocco `wrap-checkboxes` (`grid-column:span 2`, flex column, gap 10px) impilate verticalmente in ordine — id interni invariati, nessun impatto sulla logica show/hide esistente; (2) corretto testo Guida rapida sul tab Solleciti: non serve per "sollecitare pratiche ferme" ma per **registrare** solleciti già inviati agli enti (tracciamento, nessuna approvazione richiesta) — testo allineato alla label reale del tab (riga 324).
_Ultimo aggiornamento: 2026-07-04 (rev. 122) — rev.122: `mappa_impresa_caricamento.html` — allineamento a `imprese.html` (rev.113/119/120): (1) tab aggiorna pratica, stato `NO COMPETENZA` ora nasconde Nulla Osta/Polizza/Convenzione (nuovi wrap `pr-wrap-polizza`/`pr-wrap-convenzione`), reset e payload esclusi come in rev.113; (2) `pr-no-nec`/`pr-polizza`/`pr-convenzione` da `<select>` a checkbox (rev.120, stesso trade-off: sempre SI/NO tranne NO COMPETENZA, nessuno stato "—"); (3) rimosso campo "Ordinanza necessaria" (`pr-new-ord-nec`) dal form "Nuova pratica" + riga payload `ORDINANZA NECESSARIA` (rev.119). Non toccati i riferimenti `STATO_ORDINANZA`/`ordHtml` (dettaglio pratica esistente sulla mappa, feature diversa dal campo form). Bottone "?" (`openHelp()`) e Hub in questo file erano già uniformi/funzionanti da prima (nessun bug di scope: `openHelp` è già globale, fuori dalla IIFE).
_Ultimo aggiornamento: 2026-07-04 (rev. 121) — rev.121: `imprese.html` — Guida rapida (`_openHelp`): aggiunto passo mancante sul tab Solleciti.
_Ultimo aggiornamento: 2026-07-04 (rev. 120) — rev.120: `imprese.html`, tab "Aggiorna pratiche" — `fld-no-nec`/`fld-polizza`/`fld-convenzione` da `<select>` SI/NO/— a checkbox (richiesta utente). ⚠️ **Cambio di comportamento**: prima un valore vuoto ("—") significava "non specificato/non inviare"; con la checkbox non esiste stato intermedio — ora questi 3 campi vengono **sempre** inviati come SI/NO nella coda (tranne stato NO COMPETENZA, dove restano esclusi come da rev.113). Prefill checkbox da valore esistente (`=== 'SI'`). Aggiornati di conseguenza: prefill (~635), listener `fld-no-nec` (mostra/nasconde Tratte NO), reset su NO COMPETENZA, submit (`addToQueueBtn`).
_Ultimo aggiornamento: 2026-07-04 (rev. 119) — rev.119: `imprese.html` — rimosso campo "Ordinanza necessaria" da tab "Nuova pratica" (select `new-ord-nec` + riga `ORDINANZA NECESSARIA` nel payload di submit). Nessun altro riferimento a "ordinanza" nel file.
_Ultimo aggiornamento: 2026-07-04 (rev. 118) — rev.118: `imprese.html` — confermato e corretto il bug latente segnalato in rev.116: `_retryBoot()` (guard "Riprova") aveva lo stesso problema di scope di `_openHelp` (dentro la IIFE, non su `window`, `onclick` falliva). Aggiunto `window._retryBoot = _retryBoot;`. ⚠️ Nel primo tentativo di fix ho cancellato per errore la riga `function showGuard(...)` sottostante (str_replace con match troppo ampio) — rilevato subito da `node --check`, corretto nello stesso turno.
_Ultimo aggiornamento: 2026-07-04 (rev. 117) — rev.117: `imprese.html` — tasto guida: icona SVG sostituita con solo testo "?" (richiesta utente).
_Ultimo aggiornamento: 2026-07-04 (rev. 116) — rev.116: `imprese.html` — 2 fix su tasto "?" segnalati dall'utente (screenshot). (1) **Bug reale**: `_openHelp()` (rev.111) definita dentro la IIFE principale `(() => {...})()` (righe 424-1515), non esposta globalmente — `onclick="_openHelp()"` falliva con `ReferenceError`, bottone non rispondeva. Fix: aggiunto `window._openHelp = _openHelp;` dopo la definizione. ⚠️ **Stesso bug latente non toccato**: `_retryBoot()` (guard "Riprova", riga ~138) ha la stessa struttura e probabilmente lo stesso problema — mai segnalato perché il guard raramente si mostra, da verificare se serve un fix analogo. (2) Rimosso testo `?` duplicato accanto all'icona SVG cerchio-domanda (rendeva "??" visivo) — resta solo l'icona, come intended.
_Ultimo aggiornamento: 2026-07-04 (rev. 115) — rev.115: verificato `server.py` (upload utente) — `DATA_UPDATE` reale, non solo dichiarato: righe 1469-1491 (stampa su qualsiasi update pratica, guard `"DATA_UPDATE" in df.columns`) + `_touch_data_update`/`_touch_data_update_multi` (righe 1935-1975, invocate da endpoint solleciti righe 2023/2065). Usa UTC; `DATA_RICHIESTA`/`DATA_APPROVAZIONE` sono invece input utente da frontend, non timestamp server — nessun bug di disallineamento.
_Ultimo aggiornamento: 2026-07-04 (rev. 114) — rev.114: TODO §11 `DATA_UPDATE` chiuso — colonna presente in `Master.csv`, logica rev.61-62 non più no-op (confermato utente).

> **Storico rev.65–rev.113 compattato** (dettagli completi nei backup di sessione, non riportati qui). Riassunto per tema:
> - **`scavi.html` — tabella "Tutti i Cantieri" e card avanzamento** (rev.65, 77-91, 96, 101-110): decine di iterazioni di stile/larghezze colonne, icone sort/filtro, raggruppamento imprese per cluster/lotto, modal Cluster/Lotto allineate alla tabella cantieri, rimozione CSS morto (`.msc-*`, `.page-eyebrow` ecc., rev.107).
> - **`index.html` — tabella "Tutte le pratiche" e popup Note** (rev.65-75, 97-100): fix header troncati/sovrapposti, popup Note (bug dati mancanti rev.69, note multiple rev.71, rimozione solleciti dal popup rev.70 — **11.4 riaperto**), colonna Impresa nei modal (rev.75), rimozione colonna "Prev. Rilascio" e bottoni filtro scadenza (rev.98, dead code ripulito), icone sort SVG (rev.100).
> - **`imprese.html` — form Aggiorna/Nuova pratica** (rev.111-123): tasto guida "?" (bug scope IIFE rev.116/118), stato NO COMPETENZA nasconde campi non pertinenti (rev.113), select→checkbox per Nulla Osta/Polizza/Convenzione (rev.120), rimozione campo Ordinanza (rev.119).
> - **`mappa_impresa_caricamento.html`** (rev.78-80, 122): export GeoJSON lotti con esclusione campi gestionali + conferma custom brand-kit, allineamento form a `imprese.html`, rimossa `exportPDF()`.
> - **Numerazione changelog**: 2 disallineamenti rilevati e corretti (rev.74, rev.76 — sessioni parallele non intercettate da grep iniziale, contenuto invariato solo rinumerato).


> **Storico rev.10–rev.64 archiviato in `AGENT_BRIEF_ARCHIVE.md`** (audit/dettagli completi). Riassunto per tema:
> - **Rename `COORDINAMENTO`→`INVIO PRELIMINARE`** (rev.34-43, incl. 5 voci fabbricate rev.35-39 poi scartate): completato su tutti i file, chiuso rev.43 (v. §11 tabella TODO).
> - **`DATA_PREVISTA_RILASCIO`** (rev.44-48): eliminata la previsione calcolata, sostituita da campo compilato dall'impresa. Rollout completo su `Master.csv`/`server.py`/`index.html`/`imprese.html`/`mappa_impresa_caricamento.html`.
> - **Corruzione `Master.csv`** (rev.50, rev.63-64): file avvolto in virgolette esterne o con separatore/encoding sbagliato → parsing silenzioso a 1 colonna. Diagnosticato e riparato più volte (v. nuovo punto §8.40).
> - **Barra % nei modal lotto/cluster `index.html`** (rev.49, rev.51-59): 6 iterazioni di stile/posizionamento per leggibilità della percentuale sulla barra prima di arrivare alla versione finale (testo scuro sotto soglia, sempre visibile — v. §11 riga 11.9, "Risolto (rev.49)").
> - **`DATA_UPDATE`** (rev.60-62): fix sicurezza overlay login `index.html` (rev.60, chiuso) + logica frontend/backend per nuova colonna "Data Update" (rev.61-62) — **colonna CSV non ancora creata, TODO aperto** (v. §11).
> - **Fix storici `scavi.html`** (rev.10-33): tabella "Tutti i Cantieri" (sort/filtro colonne, larghezze, registro cantiere), donut/KPI "Distribuzione Stati"/"Lotti attivi", rebranding Retelit, keepalive Render (cron-job.org, rev.27-28), guardia `pageshow`/bfcache su 11 pagine (rev.26 — ha anche scoperto il gap di sicurezza `milestone.html`/`admin.html` tuttora aperto, v. §11).

_Versione compattata il 2026-07-06: changelog rev.65-113 compattato in riassunto tematico (rev.10-64 già in `AGENT_BRIEF_ARCHIVE.md` da compattazione precedente). Rev.137: pulizia riferimenti a file rimossi (`mappa_impresa.html`) + correzione tabella auth §5.1 obsoleta + audit pre-produzione 3 pagine impresa (nessun bug bloccante). Restano inline rev.114-137 (dettagliate, più recenti/actionable).
